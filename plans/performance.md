# MustardScript Performance Plan

## Objective

Make `mustardscript` materially faster without weakening correctness, limits,
cancellation, snapshot validation, or the "Rust core / thin Node wrapper"
contract. Every optimization milestone must land with benchmark evidence, not
just profiler intuition.

## Audited Baseline

Current audited benchmark inputs and evidence:

- Existing end-to-end harness: `benchmarks/smoke.ts` and `benchmarks/workloads.ts`
- Latest checked-in workload report:
  `benchmarks/results/2026-04-13T13-38-06-436Z-workloads.json`
- Latest findings summary: `docs/BENCHMARK_FINDINGS.md`
- Current smoke gate status on April 13, 2026:
  `npm run bench:smoke:dev` and `npm run bench:smoke:release` both pass with
  profile-specific median/p95 budgets and write result artifacts.

Current addon medians from the checked-in workload report:

| Metric | Current |
| --- | ---: |
| `cold_start_small` | `1.02 ms` |
| `warm_run_small` | `0.96 ms` |
| `cold_start_code_mode_search` | `0.95 ms` |
| `warm_run_code_mode_search` | `0.54 ms` |
| `programmatic_tool_workflow` | `1.69 ms` |
| `host_fanout_10` | `0.05 ms` |
| `host_fanout_100` | `0.39 ms` |
| `suspend_resume_20` | `2.23 ms` |

Current sidecar/addon overhead from the same report:

- `cold_start_small`: `10.49x`
- `programmatic_tool_workflow`: `11.15x`
- `host_fanout_100`: `17.05x`

Current relative position versus the isolate baseline:

- `mustard` is still slower on raw execution throughput
- addon mode is now much closer on host-call-heavy paths, but the isolate still
  wins at larger synchronous fanout counts
- `mustard` is already better on suspend/resume workloads
- the next phase should focus on 2x to 6x gains in addon mode before trying to
  close the entire isolate gap

## Audited Hotspots

These are concrete code-level reasons the current runtime is paying avoidable
cost:

- `lib/runtime.ts` stores a serialized program buffer in `Mustard`, so warm
  runs still cross the addon boundary with encoded bytecode instead of a native
  compiled-program handle.
- `crates/mustard/src/runtime/mod.rs` rebuilds a fresh runtime image on every
  execution, including builtin installation, global setup, capability globals,
  and input conversion.
- `crates/mustard-node/src/lib.rs` decodes that program buffer on every
  `start_program` call and moves start/resume traffic through JSON strings.
- `crates/mustard/src/runtime/api.rs` validates bytecode again and clones the
  full `BytecodeProgram` into `Runtime::new()` on each execution.
- `crates/mustard/src/runtime/vm.rs` clones the current `Instruction` before
  dispatch, which also clones opcode payloads such as `String`s.
- `crates/mustard/src/runtime/async_runtime/scheduler.rs` clones the whole
  `Runtime` to produce a suspension snapshot before serialization even begins.
- `crates/mustard/src/runtime/state.rs` stores the full `BytecodeProgram` inside
  `Runtime`, so snapshots repeatedly serialize immutable program bytes.
- `lib/progress.ts` and `lib/policy.ts` keep rehashing snapshots, rebuilding
  auth tokens, and reserializing policy metadata on hot suspend/resume paths,
  while also using a synchronous filesystem-based single-use registry.
- `crates/mustard/src/runtime/mod.rs` routes synchronous builtin callbacks
  through promise machinery even when host suspension is forbidden.
- `crates/mustard/src/runtime/gc.rs` also uses `HashSet`-heavy marking, making
  each individual collection more expensive than it needs to be.
- `crates/mustard/src/runtime/accounting.rs` frequently remeasures whole
  objects/arrays/maps/sets after local mutations instead of applying deltas.
- `crates/mustard/src/runtime/env.rs` resolves names by walking env chains and
  doing string lookups on hot paths, while globals are duplicated across the
  globals env and global object.
- `crates/mustard/src/runtime/builtins/collections.rs` backs `Map` and `Set`
  with vectors and linear scans, and delete/clear paths scan live iterators to
  repair indices.
- `lib/structured.ts`, `crates/mustard-bridge/src/codec.rs`, and
  `crates/mustard-bridge/src/operations.rs` use recursive JS DTO construction,
  JSON, and base64 in places where addon mode could stay binary end to end.

## North-Star Targets

These are cumulative targets for addon mode on the current benchmark suite.
Each has a realistic floor and an ambitious stretch target.

| Metric | Current | Floor Target | Stretch Target |
| --- | ---: | ---: | ---: |
| `cold_start_small` | `17.81 ms` | `<= 8.0 ms` | `<= 4.0 ms` |
| `warm_run_small` | `16.89 ms` | `<= 4.0 ms` | `<= 2.0 ms` |
| `cold_start_code_mode_search` | `39.00 ms` | `<= 18.0 ms` | `<= 10.0 ms` |
| `warm_run_code_mode_search` | `38.65 ms` | `<= 12.0 ms` | `<= 6.0 ms` |
| `programmatic_tool_workflow` | `42.47 ms` | `<= 10.0 ms` | `<= 5.0 ms` |
| `host_fanout_100` | `6.67 ms` | `<= 1.5 ms` | `<= 0.8 ms` |
| `suspend_resume_20` | `3.64 ms` | `<= 2.0 ms` | `<= 1.0 ms` |

North-star sidecar targets:

- keep sidecar slower than addon, but reduce `sidecar/addon` ratio to
  `<= 1.20x` on `warm_run_small`, `programmatic_tool_workflow`, and
  `host_fanout_100`
- preserve the current suspend/resume advantage over the isolate baseline

## Measurement Rules

- [x] Add a Rust-core microbenchmark suite under `crates/mustard/benches` for:
  parse/lower, `load_program`, `start_bytecode`, VM hot loops, env lookup,
  property access, `Map`/`Set`, structured boundary encode/decode, and
  snapshot dump/load.
- [x] Add phase-split benchmarks for `runtime_init_only`, `execution_only`,
  `suspend_only`, `snapshot_dump_only`, `snapshot_load_only`,
  `apply_snapshot_policy_only`, and `Progress.load_only`.
- [x] Keep `npm run bench:smoke` as the fast local gate, but stop using current
  dev-profile absolute thresholds as the main optimization signal.
- [x] Add a release-profile smoke command and treat release medians as the
  source of truth for performance decisions.
- [x] Require every optimization PR to attach before/after numbers from
  `npm run bench:workloads` plus the new Rust microbench suite.
- [x] Record machine metadata, build profile, git SHA, and benchmark fixture
  version in every result artifact.
- [x] Record snapshot byte size, serialized program byte size, and retained live
  heap size for suspend/resume-heavy workloads.
- [x] Add boundary-only benchmark coverage for start inputs, suspended args,
  resume values, and resume errors with small, medium, and large nested host
  values.
- [x] Fail regression checks on relative regressions against a checked-in
  baseline, not on stale absolute budgets.
- [x] Update `docs/BENCHMARK_FINDINGS.md` only after rerunning the full release
  benchmark suite.

## Milestone 0: Stabilize Benchmarking And Baselines

Target by end of milestone:

- benchmark tooling is trustworthy enough to drive engineering decisions
- smoke benchmarks pass consistently in their intended profile
- there is one checked-in baseline for core, addon, and sidecar performance

Action items:

- [x] Split benchmark commands into explicit `dev` and `release` variants.
- [x] Fix or replace the current smoke budgets so they reflect measured reality;
  the current April 12, 2026 smoke run already fails.
- [x] Add a simple benchmark comparison script that diff-checks medians and p95
  against the latest baseline JSON.
- [x] Capture a fresh release baseline on the current branch and store it under
  `benchmarks/results/`.
- [x] Extend `benchmarks/workloads.ts` to include a direct "execution only"
  measurement that excludes compile/decode overhead.
- [x] Extend `benchmarks/workloads.ts` to include `runtime_init_only`,
  `suspend_only`, `snapshot_dump_only`, `snapshot_load_only`,
  `apply_snapshot_policy_only`, and `Progress.load_only` measurements.
- [x] Document the benchmark protocol in `benchmarks/README.md`.

## Milestone 1: Remove Structural Start/Run Overhead

Why this comes first:

- warm runs are almost as expensive as cold runs, which strongly suggests the
  runtime is paying start-path overhead on every execution
- the audited code confirms repeated decode, validate, and clone work

Target by end of milestone:

- `warm_run_small <= 10 ms`
- `warm_run_code_mode_search <= 28 ms`
- `programmatic_tool_workflow <= 32 ms`

Action items:

- [x] Add a native compiled-program handle so `Mustard` can hold decoded,
  validated bytecode instead of a serialized buffer.
- [x] Make `Runtime` start from shared immutable program state rather than
  cloning `BytecodeProgram` per execution.
- [x] Introduce a reusable immutable runtime image for builtins, global object
  state, and other stable startup heap data, with copy-on-write or equivalent
  isolation for guest-visible mutation.
- [x] Introduce a reusable execution-context handle for stable capabilities and
  limits so repeated runs do not rebuild policy state from scratch.
- [x] Remove or drastically reduce duplicated globals bookkeeping between the
  globals env and the global object on hot startup and assignment paths.
- [x] Validate bytecode once at compile/load boundaries and skip redundant
  validation for trusted in-process compiled handles.
- [x] Separate "compile + validate", "deserialize + validate", and "execute"
  benchmarks so the win is visible in isolation.
- [x] Add explicit `runtime_init_empty`, `runtime_init_with_capabilities`, and
  `runtime_init_with_inputs` microbenches.
- [x] Re-run addon, sidecar, and Rust-core benchmarks and store the before/after
  result set.

## Milestone 2: Accelerate Suspend/Resume And Snapshot Lifecycle

Why this comes second:

- `mustard` already wins on resumable execution, so this is the highest-leverage
  place to widen an existing product advantage
- the reviewed code shows large fixed costs inside suspension and restore before
  transport overhead is even counted

Target by end of milestone:

- `suspend_resume_20 <= 2.8 ms`
- `host_fanout_100 <= 5.0 ms`
- snapshot size for "large program, tiny live heap" workloads reduced by at
  least `70%`

Action items:

- [x] Stop cloning the whole `Runtime` to create suspension snapshots; freeze or
  otherwise capture suspended state without aliasing bugs.
- [x] Externalize immutable `BytecodeProgram` data from snapshots so resume uses
  `snapshot state + program identity` rather than repeating stable program
  bytes.
- [x] Replace raw snapshot round-trips with opaque snapshot handles and cached
  auth metadata where the addon or sidecar session can safely retain live
  suspended state.
- [x] Replace `Progress.load()` re-inspection with an authenticated suspended
  manifest fast path, while keeping legacy snapshots on the slow validation
  path.
- [x] Remove the duplicated filesystem-based single-use registry in
  `Progress` or replace it with one shared mechanism instead of two.
- [x] Add incremental or cached snapshot-policy and post-load accounting checks
  where possible, with debug-mode full-walk assertions to preserve fail-closed
  behavior.
- [x] Add Rust and JS benchmarks for `suspend_only`, `dump_only`, `load_only`,
  `policy_only`, and `Progress.load_only`.

## Milestone 3: Speed Up The Interpreter, Symbol Paths, And Sync Callbacks

Why this comes third:

- once startup and suspend-path churn are reduced, dispatch, symbol lookup, and
  callback machinery will dominate addon latency
- the current runtime still pays a large amount of avoidable `String` cloning
  and promise machinery overhead on common synchronous helper paths

Target by end of milestone:

- `warm_run_small <= 6 ms`
- `warm_run_code_mode_search <= 20 ms`
- `programmatic_tool_workflow <= 24 ms`

Action items:

- [x] Stop cloning `Instruction` on every dispatch; borrow from the current
  function’s code slice instead.
- [ ] Expand the constant-pool plan into runtime-wide string/key interning or
  symbol IDs so env bindings, property names, capability names, and hot string
  values stop paying full `String` costs everywhere.
- [x] Add resolved local/cell slot opcodes for lexical bindings and closure
  captures so hot local loads/stores do not walk env chains by name.
- [x] Keep slow-path name lookup for globals and diagnostics, but move ordinary
  local access and common global/property cases onto a separate fast path.
- [x] Add a true synchronous callback fast path so builtin helpers such as
  `Array.map`, `Array.reduce`, `Map.forEach`, `Set.forEach`, and string
  callbacks do not route through promise machinery when suspension is forbidden.
- [x] Add targeted microbenches for tight loops, local loads/stores, closure
  access, static property get/set, builtin method access, and callback-heavy
  array/collection workloads.

## Milestone 4: Reduce GC, Async Clone, And Accounting Overhead

Why this comes fourth:

- current GC policy is correctness-first and visibly too eager
- current accounting refreshes are accurate but overly expensive
- async promise settlement is still clone-heavy and can multiply allocation
  costs under fanout workloads

Target by end of milestone:

- `warm_run_small <= 5 ms`
- `warm_run_code_mode_search <= 16 ms`
- `programmatic_tool_workflow <= 18 ms`
- addon retained heap after the workflow benchmark stays `<= 128 KiB`

Action items:

- [x] Replace "collect before every maybe-allocating instruction" with an
  allocation-debt or threshold-based trigger.
- [x] Replace `HashSet`-heavy GC marking with epoch/bit-mark or equivalent
  cheaper reachability tracking so each individual collection is faster.
- [x] Keep limit enforcement fail-closed even when GC is deferred.
- [x] Convert array/object/map/set/env/promise accounting from full remeasure to
  incremental deltas wherever the exact byte delta is knowable.
- [x] Avoid full heap-total recomputation on every collection; reserve full
  recounts for snapshot load, validation repair, or debug assertions.
- [ ] Reduce async clone amplification in promise settlement, awaiter
  scheduling, and reaction dispatch, especially for `Promise.all` and other
  fanout-heavy flows.
- [x] Add benchmark counters for collection count, total GC time, reclaimed
  bytes, and accounting refresh frequency.
- [x] Add correctness tests for heap-limit failure timing, cancellation, and
  snapshot restore under the new GC trigger policy.

## Milestone 5: Optimize The Addon Host Boundary And Execution Context

Why this comes fifth:

- the current addon bridge is paying JSON serialization and snapshot base64
  costs that do not buy semantic value in-process
- the current host-boundary cost is broader than JSON alone because JS builds
  recursive structured DTO objects before Rust sees the data
- this should directly improve host fanout and workflow workloads

Target by end of milestone:

- `host_fanout_10 <= 0.35 ms`
- `host_fanout_100 <= 2.5 ms`
- `programmatic_tool_workflow <= 12 ms`
- `limit_failure` and `host_failure` recovery medians `<= 8 ms`

Action items:

- [ ] Replace JSON-string start/resume payloads with typed N-API values or a
  binary DTO path.
- [ ] Eliminate the JS-side structured-value DTO hot path where possible, while
  preserving the same fail-closed boundary rules for proxies, accessors, sparse
  arrays, and non-finite numbers.
- [x] Remove base64 encoding from in-process snapshot transport; keep raw
  `Buffer` snapshots in addon mode.
- [x] Cache or reuse encoded policy/capability metadata across repeated run and
  resume calls.
- [x] Promote policy/capability caching into explicit native execution-context
  handles for workloads that repeatedly execute with stable host handlers and
  limits.
- [x] Benchmark structured boundary encode/decode cost separately from VM
  execution cost.
- [x] Keep the Node wrapper thin; all semantic validation and snapshot policy
  enforcement must remain in Rust.

## Milestone 6: Upgrade Core Data Structures, Globals, And Property Fast Paths

Why this comes sixth:

- large collections and property-heavy workloads will remain expensive even
  after the interpreter and bridge are faster

Target by end of milestone:

- `warm_run_code_mode_search <= 12 ms`
- `programmatic_tool_workflow <= 10 ms`
- large `Map`/`Set` membership and update microbenches improve by at least `5x`

Action items:

- [x] Replace vector-backed `Map`/`Set` lookup/update paths with an
  order-preserving hashed representation that still respects SameValueZero and
  iterator semantics.
- [x] Redesign `Map`/`Set` iterator invalidation so delete/clear no longer scan
  the full live iterator set to repair indices.
- [x] Add large-collection benchmarks for `Map.get`, `Map.set`, `Map.has`,
  `Set.add`, `Set.has`, `Set.delete`, and iterator throughput.
- [x] Add fast paths for common static property cases such as array length,
  plain-object own property hits, and builtin prototype method access.
- [x] Add capacity-aware builders and bulk-mutation fast paths for
  arrays/objects/maps/sets/promises so constructors and append-heavy code stop
  remeasuring full structures after tiny local changes.
- [x] Finish removing duplicated globals env/global-object work if it was not
  completed in Milestone 1.
- [x] Audit builtin helpers for avoidable cloning and temporary allocation.
- [x] Re-run the keyed-collection and property correctness suites after each
  internal representation change.

## Milestone 7: Reduce Sidecar Transport Overhead

Why this comes seventh:

- sidecar is a deployment choice, not the first optimization target
- once addon mode is faster, the remaining sidecar gap becomes easier to see

Target by end of milestone:

- sidecar/addon ratio `<= 1.20x` on `warm_run_small`
- sidecar/addon ratio `<= 1.20x` on `programmatic_tool_workflow`
- sidecar/addon ratio `<= 1.20x` on `host_fanout_100`

Action items:

- [x] Replace line-delimited JSON/base64 framing with length-prefixed binary
  messages while keeping a debug-friendly inspection mode.
- [x] Keep program and snapshot bytes binary throughout the sidecar protocol.
- [x] Reuse compiled program state inside long-lived sidecar sessions instead of
  recompiling or redecoding unnecessarily.
- [x] Extend session statefulness to `program_id`, `snapshot_id`, and compact
  policy/capability IDs so the sidecar stops resending large opaque blobs and
  static metadata on every request.
- [x] Split sidecar startup, transport, and execution costs into separate
  benchmark metrics.
- [x] Preserve protocol hardening tests and explicit versioning for any wire
  format change.

## Milestone 8: Bytecode And Lowering Specialization

Why this is last:

- this is higher complexity than the earlier "obvious waste" removals
- it should only start once the simpler structural wins flatten out

Target by end of milestone:

- hit or beat the floor targets in the north-star table
- stretch goal: hit at least half of the stretch targets in the north-star table

Action items:

- [ ] Reframe compiler optimization around structural lowering wins first:
  object literals, destructuring, and `for...of` should emit materially leaner
  bytecode before adding generic peephole machinery.
- [ ] Add peephole passes to remove stack churn such as redundant `Dup`/`Pop`
  pairs and other common bytecode noise.
- [ ] Introduce superinstructions for the most common opcode sequences shown by
  profiling.
- [ ] Constant-fold and simplify obviously static bytecode at lower time.
- [x] Evaluate monomorphic inline caches for static property reads only after
  benchmark evidence shows property dispatch is still dominant.
- [ ] Keep any specialization optional enough that diagnostics, validation, and
  snapshot compatibility remain understandable.

## Completion Criteria

- [ ] `cargo test --workspace` passes after each substantial milestone.
- [ ] `npm test` passes after each substantial milestone.
- [ ] `npm run lint` passes after each substantial milestone.
- [ ] `npm run bench:smoke` passes in its intended profile.
- [ ] `npm run bench:workloads` shows the milestone’s promised wins.
- [ ] Each completed milestone has a checked-in benchmark artifact and a short
  written result summary.
- [ ] No milestone is marked complete without explicit before/after numbers for
  the metrics it targeted.

## Iteration Log

| UTC Timestamp | Commit | Summary | Errors / Blockers |
| --- | --- | --- | --- |
| 2026-04-13T06:44:27Z | `fac215a` (worktree dirty) | Audited the benchmark/runtime/boundary hot paths, wrote the initial performance roadmap, and folded parallel sub-agent review findings into the milestone structure. | `npm run bench:smoke` currently fails with `compute average 52.76ms exceeded 25ms`; no external blocker identified. |
| 2026-04-13T07:17:00Z | `125fcb6`, `2b5bf99` | Completed Milestone 0 benchmark stabilization: added explicit dev/release benchmark commands, artifact metadata, release smoke coverage, phase-split addon metrics, a benchmark diff script, a fresh checked-in release baseline, and updated findings/docs. Then started Milestone 1 by switching addon execution to validated native program handles plus shared `Arc<BytecodeProgram>` state so in-process runs stop re-deserializing and re-cloning program bytecode on every start path. | Full verification passed (`npm test`, `cargo test --workspace`, `npm run lint`, smoke/workload benchmark commands). Release workload deltas for the Milestone 1 groundwork were small and mixed (`programmatic_tool_workflow -1.8%`, `host_fanout_100 -1.9%`, `warm_run_small +1.0%` versus the new baseline), so the next concrete path is reusable runtime-image/startup-state work. A temporary `bench:smoke`/`bench:workloads` parallel rerun hit a local `scripts/build-ts-dist.ts` `ENOTEMPTY` race on `dist/`; rerunning sequentially resolved it. |
| 2026-04-13T07:30:55Z | `a6ec42d` | Added the Rust-core microbenchmark suite under `crates/mustard/benches/runtime_core.rs`, wired `npm run bench:rust`, documented the required workload-plus-Rust perf evidence workflow, and covered compile/lower, deserialize/validate, runtime init variants, start-vs-execute, VM hot paths, structured boundary encode/decode, and snapshot dump/load. | Verification passed (`npm run bench:rust`, `cargo test --workspace`, `npm test`, `npm run lint`). `npm test` initially failed in `tests/package-smoke.test.js` because the packed source tarball omitted `crates/mustard/benches/runtime_core.rs` while `crates/mustard/Cargo.toml` declared the bench; fixed by adding `crates/mustard/benches/**` to `package.json` `files`. |
| 2026-04-13T07:41:57Z | `49d891f` | Reduced Milestone 1 startup overhead by keeping builtins, capabilities, and inputs on the real global object instead of duplicating globals-env cells, added a regression test for global-object lookup/mutation of inputs and capabilities, and removed the redundant `start(&CompiledProgram, ...)` bytecode revalidation after lowering. Checked-in release artifact `benchmarks/results/2026-04-13T07-40-15-477Z-workloads.json` improved versus `2026-04-13T06-59-51-780Z`: addon `cold_start_small -33.5%`, `warm_run_small -32.6%`, `programmatic_tool_workflow -11.7%`, `host_fanout_100 -20.5%`, `runtime_init_only -24.4%`, and `execution_only_small -23.6%`. | Verification passed (`npm run bench:rust`, `npm run bench:workloads:release`, `cargo test --workspace`, `npm test`, `npm run lint`). No external blocker identified; the next concrete Milestone 1 path is a reusable immutable runtime image plus execution-context caching. |
| 2026-04-13T07:58:44Z | `00510fe` (worktree dirty) | Added a cached Rust runtime image for builtin/global startup state, reused it for each `Runtime::new()` with explicit baseline heap/allocation checks, added integration coverage that zero limits still fail closed and cross-run builtin/global mutations do not leak, checked in `benchmarks/results/2026-04-13T07-55-31-500Z-workloads.json` plus `2026-04-13T07-55-40-075Z-smoke-release.json`, and refreshed `docs/BENCHMARK_FINDINGS.md` with the new evidence. Versus `2026-04-13T07-40-15-477Z`, addon medians improved modestly on `warm_run_small -2.5%`, `warm_run_code_mode_search -1.3%`, `programmatic_tool_workflow -0.8%`, and `host_fanout_100 -2.0%`, while the Rust-core startup microbenches improved much more (`runtime_init_empty ~-37%`, `runtime_init_with_capabilities ~-35%`, `runtime_init_with_inputs ~-24%`). | Verification passed (`cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:rust`, `npm run bench:workloads:release`, `npm run bench:smoke:release`). The first `npm test` attempt hit a transient `tests/package-smoke.test.js` tarball `ENOENT`; rerunning `node --test tests/package-smoke.test.js` and then `npm test` sequentially passed. `execution_only_small` regressed slightly (`+1.9%`), so reusable execution-context caching or interpreter hot-path work remains the next concrete path. |
| 2026-04-13T08:07:48Z | `d9c52a1` | Changed VM dispatch to borrow instructions from an `Arc<BytecodeProgram>` clone instead of cloning the full `Instruction` every step, refreshed `docs/BENCHMARK_FINDINGS.md`, and checked in `benchmarks/results/2026-04-13T08-04-08-003Z-workloads.json` plus `2026-04-13T08-06-50-446Z-smoke-release.json`. Versus `2026-04-13T07-55-31-500Z`, addon medians improved on `cold_start_small -4.0%`, `warm_run_small -3.2%`, `warm_run_code_mode_search -0.8%`, and `programmatic_tool_workflow -1.4%`; the Rust-core bench showed bigger wins (`execute_shared_small_compute ~-8.4%`, `vm_hot_loop ~-8.8%`, `env_lookup_hot ~-6.4%`, `property_access_hot ~-4.9%`). | Verification passed (`cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:rust`, `npm run bench:workloads:release`, `npm run bench:smoke:release`). No external blocker identified. `execution_only_small` and `host_fanout_100` stayed roughly flat (`+0.6%` and `+0.4%`), so execution-context caching or slot-based lexical fast paths remain the next concrete path. |
| 2026-04-13T08:48:20Z | `d9c52a1` (worktree dirty) | Tried a true synchronous callback fast path, benchmarked it, saw no meaningful win, and backed it out. Kept the measurable follow-on work instead: added `array_callback_hot` plus a callback-throw regression test, cached per-function parameter/rest binding names in bytecode so `Runtime::push_frame` stops re-walking destructuring patterns on every call, and added bytecode validation that rejects forged binding metadata. Checked in `benchmarks/results/2026-04-13T08-36-39-534Z-workloads.json` plus `2026-04-13T08-38-48-635Z-smoke-release.json`. Against a control worktree at `d9c52a1`, the new `array_callback_hot` bench improved from `26.13-26.50 ms` to `25.20-25.52 ms` (`~3-4%`), while the release workload suite stayed mixed but `execution_only_small` still improved from `13.83 ms` to `13.60 ms`. | Verification passed (`cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:rust`, `npm run bench:workloads:release`, `npm run bench:smoke:release`). No external blocker identified. Broader workload gains remained noisy (`programmatic_tool_workflow +4.0%` versus `2026-04-13T08-04-08-003Z`), so reusable execution-context caching or slot-based lexical fast paths remain the next concrete path. |
| 2026-04-13T09:05:03Z | `4814c3e` (worktree dirty) | Completed the remaining Milestone 1 execution-context work by adding a reusable Node `ExecutionContext` handle, wiring `Mustard.run()`, `start()`, and `Progress.load()` to accept it, updating the benchmark harness to reuse stable addon policy state, and adding Node/docs/type coverage for both the reusable path and the fail-closed mixed-options path. Checked in `benchmarks/results/2026-04-13T09-02-17-086Z-workloads.json` plus `2026-04-13T09-02-30-985Z-smoke-release.json`. Versus `2026-04-13T08-36-39-534Z`, addon `runtime_init_only` improved from `0.07 ms` to `0.04 ms` (`-43%`), `execution_only_small` improved from `13.60 ms` to `13.45 ms` (`-1.0%`), `programmatic_tool_workflow` improved from `37.98 ms` to `36.95 ms` (`-2.7%`), and `suspend_resume_20` improved from `3.19 ms` to `3.10 ms` (`-2.9%`), while `warm_run_small` and `host_fanout_100` regressed slightly (`+1.4%` and `+1.5%`). | Verification passed (`cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:rust`, `npm run bench:workloads:release`, `npm run bench:smoke:release`). No external blocker identified. The repeated-addon startup path is now materially cheaper, but broader latency is still mixed, so the next concrete path is Milestone 2 snapshot/suspend lifecycle work or Milestone 3 local-slot fast paths. |
| 2026-04-13T09:19:17Z | `aa543d5` (worktree dirty) | Completed the first Milestone 2 suspend-path capture item by replacing sync and async suspension-time `Runtime` clones with a move-based `ExecutionSnapshot::capture(...)` handoff, removing the extra `dump_snapshot()` runtime clone, and adding an exercised integration test that round-trips async host suspensions through dump/load/resume. Checked in `benchmarks/results/2026-04-13T09-17-14-652Z-workloads.json` plus `2026-04-13T09-17-27-082Z-smoke-release.json`. Relative to `2026-04-13T09-02-17-086Z`, addon `host_fanout_100` improved from `5.23 ms` to `4.77 ms` (`-8.9%`), `programmatic_tool_workflow` improved from `36.95 ms` to `35.20 ms` (`-4.7%`), `suspend_resume_20` improved from `3.10 ms` to `3.06 ms` (`-1.3%`), and release smoke snapshot ratio improved from `1.51x` to `1.45x` (`-3.7%`). The clearest direct signal came from `npm run bench:rust`, where `snapshot_dump_suspended` improved by about `58%`. | Verification passed (`cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:rust`, `npm run bench:workloads:release`, `npm run bench:smoke:release`). `npm run lint` initially failed on Clippy `clone_on_copy` in `crates/mustard/src/runtime/api.rs`; fixing the helper and rerunning lint resolved it. No external blocker identified. The next concrete Milestone 2 path is externalizing immutable `BytecodeProgram` data from snapshots so large-program snapshots stop carrying stable program bytes. |
| 2026-04-13T09:31:06Z | `4ca5cac` (worktree dirty) | Completed the measurement-rule gap for suspend-heavy workloads by extending `benchmarks/workloads.ts` to record addon `suspendState` facts for each `suspend_resume_*` fixture: serialized program bytes, dumped snapshot bytes, and retained live `Progress` heap/RSS deltas while holding `20` suspended executions live. Added compare-tool coverage proving the new non-latency section is ignored by benchmark diffs, updated `benchmarks/README.md`, refreshed `docs/BENCHMARK_FINDINGS.md`, and checked in `benchmarks/results/2026-04-13T09-28-45-001Z-workloads.json` plus `2026-04-13T09-29-05-358Z-smoke-release.json`. The new artifact shows `457 B` serialized programs and `3,195 B` dumped snapshots for the current `suspend_resume_*` fixtures, with retained live heap around `22 KB` for the `5`- and `20`-boundary cases. | Verification passed (`node --test tests/node/benchmark-compare.test.js`, `npm test`, `npm run lint`, `npm run bench:workloads:release`, `npm run bench:smoke:release`). No external blocker identified. The retained live-heap sample for `suspend_resume_1` stayed slightly negative after GC, so that one data point should be treated as allocator noise; the `5`- and `20`-boundary measurements produced the useful positive signal needed for the next snapshot-size reduction work. |
| 2026-04-13T09:49:50Z | `2e754a3` (worktree dirty) | Completed the `Progress.load()` Milestone 2 fast path by adding authenticated suspended-manifest metadata to dumped progress state, verifying that manifest in JS so current dumps skip native `inspectSnapshot(...)`, keeping legacy dumps on the inspection fallback, preserving load-time unauthorized-capability rejection, and updating executor persistence plus public/docs typing to retain the new metadata. Checked in `benchmarks/results/2026-04-13T09-45-36-897Z-workloads.json` plus `2026-04-13T09-45-59-333Z-smoke-release.json`. Relative to `2026-04-13T09-28-45-001Z`, addon `Progress.load_only` improved from `0.123 ms` to `0.097 ms` (`-21.1%`), `suspend_resume_20` improved from `3.06 ms` to `2.73 ms` (`-10.7%`), `snapshot_load_only` stayed flat at about `0.02 ms`, and the release smoke snapshot ratio improved from `1.49x` to `1.28x` (`-14.3%`). | Verification passed (`node --test tests/node/progress.test.js tests/node/security-progress-load.test.js tests/node/executor.test.js`, `npm test`, `npm run lint`, `cargo test --workspace`, `npm run bench:workloads:release`, `npm run bench:smoke:release`). A narrower exploratory run of `node --test tests/hardening/mutation-guards.test.js` exposed unrelated stale expectations around default parameters and policy-free `Progress.load(...)`; those failures are pre-existing and outside the current plan item. No external blocker identified. The next concrete Milestone 2 path is externalizing immutable `BytecodeProgram` data from snapshots so large-program snapshots stop carrying stable program bytes. |
| 2026-04-13T10:00:07Z | `bd4d52b` (worktree dirty) | Replaced the previous double-claim single-use enforcement path with one shared filesystem-backed progress registry: removed the extra native `claim/is/releaseProgressSnapshot` registry, kept the cross-thread/cross-package lockfile authority, and left the external `Progress` lifecycle semantics unchanged. Refreshed `docs/BENCHMARK_FINDINGS.md` and checked in `benchmarks/results/2026-04-13T09-59-36-458Z-workloads.json` plus `2026-04-13T09-59-48-548Z-smoke-release.json`. Relative to `2026-04-13T09-45-36-897Z`, addon `Progress.load_only` improved from `0.097 ms` to `0.092 ms` (`-5.9%`) and `apply_snapshot_policy_only` improved from `0.015 ms` to `0.013 ms` (`-13.7%`), while `host_fanout_100` stayed flat (`+0.2%`) and `suspend_resume_20` regressed slightly (`+2.0%`). | Verification passed (`npm run build`, `node --test tests/node/progress.test.js tests/node/security-progress-load.test.js tests/node/executor.test.js`, `cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:workloads:release`, `npm run bench:smoke:release`). No external blocker identified. The next concrete Milestone 2 path is still externalizing immutable `BytecodeProgram` data from snapshots so large-program snapshots stop carrying stable program bytes. |
| 2026-04-13T10:14:09Z | `e3714ee` (worktree dirty) | Completed the remaining Measurement Rules boundary-coverage gap by extending `benchmarks/workloads.ts` with addon-only boundary benchmarks for `startInputs`, `suspendedArgs`, `resumeValues`, and `resumeErrors` across small/medium/large nested payloads, updating benchmark docs/tests, and checking in `benchmarks/results/2026-04-13T10-10-36-682Z-workloads.json` plus `2026-04-13T10-11-10-782Z-smoke-release.json`. The new artifact shows the intended hotspot clearly: large `suspendedArgs` is `15.30 ms`, while large `startInputs`, `resumeValues`, and `resumeErrors` stay at `0.86 ms`, `0.81 ms`, and `0.89 ms`. | Verification passed (`node --test tests/node/benchmark-compare.test.js`, `npm run bench:workloads:release`, `npm run bench:smoke:release`, `cargo test --workspace`, `npm test`, `npm run lint`). The first `npm run bench:workloads:release` attempt failed because the new `resumeErrors` benchmark returned `undefined`; rewriting the fixture to store the catch-path result before the final expression fixed it. No external blocker identified. The next earliest open benchmarking path is relative regression checks against a checked-in baseline, and the next core runtime path remains externalizing immutable `BytecodeProgram` data from snapshots. |
| 2026-04-13T10:18:24Z | `cbbe343` (worktree dirty) | Completed the remaining benchmarking-regression gate by teaching the benchmark compare flow to resolve baselines from git-tracked artifacts, adding prefix-scoped regression filtering, and wiring `npm run bench:regress:workloads` plus `npm run bench:regress:smoke` so release checks now fail on relative regressions against checked-in baseline artifacts instead of relying only on absolute smoke budgets. | Verification passed (`node --test tests/node/benchmark-compare.test.js`, `npm run bench:regress:workloads`, `npm run bench:regress:smoke`, `cargo test --workspace`, `npm test`, `npm run lint`). No external blocker identified. The next earliest feasible runtime path remains externalizing immutable `BytecodeProgram` data from snapshots so large-program snapshots stop carrying stable program bytes. |
| 2026-04-13T10:45:51Z | `e7f5bb3` | Completed the next Milestone 2 runtime chunk by externalizing immutable compiled-program data from current addon snapshots: added detached Rust snapshot serialization with program-identity checks, native retain/inspect/resume support for detached snapshots, JS `Progress.dump()` / `Progress.load()` support for detached `program` + `program_id` state while keeping legacy self-contained dumps on the slow path, refreshed docs/types/tests, and checked in `benchmarks/results/2026-04-13T10-41-58-990Z-workloads.json` plus `2026-04-13T10-42-03-386Z-smoke-release.json`. Relative to `2026-04-13T10-10-36-682Z`, addon suspend snapshots shrank from `3,195 B` to `2,774 B` (`-13.2%`), `warm_run_small` improved from `9.90 ms` to `9.69 ms` (`-2.1%`), while `programmatic_tool_workflow` regressed from `34.34 ms` to `35.23 ms` (`+2.6%`) and `suspend_resume_20` moved from `2.58 ms` to `2.64 ms` (`+2.3%`). | Verification passed (`cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:rust`, `npm run bench:workloads:release`, `npm run bench:smoke:release`). The first `npm run bench:workloads:release` attempt failed because `benchmarks/workloads.ts` still measured detached addon snapshots through legacy `inspectSnapshot(...)`; switching that phase to `inspectDetachedSnapshot(...)` fixed the release benchmark. No external blocker identified. The next concrete Milestone 2 path is replacing raw detached snapshot round-trips with opaque snapshot handles and cached auth metadata. |
| 2026-04-13T11:11:48Z | `86b4956` (worktree dirty) | Completed the next Milestone 2 runtime chunk by adding opaque native snapshot handles for live addon `Progress` state, moving `run()` / `start()` / `resume()` onto that handle path, applying explicit snapshot policy once when `Progress.load(...)` rebuilds a live handle, and updating the benchmark/docs/test coverage around the new semantics. Checked in `benchmarks/results/2026-04-13T11-03-11-141Z-workloads.json` plus `2026-04-13T11-04-38-864Z-smoke-release.json`. Relative to `2026-04-13T10-41-58-990Z`, addon medians improved on `programmatic_tool_workflow -36.3%` (`35.23 ms -> 22.45 ms`), `host_fanout_10 -78.6%` (`0.53 ms -> 0.11 ms`), `host_fanout_100 -79.0%` (`4.79 ms -> 1.01 ms`), and `suspend_resume_20 -9.1%` (`2.64 ms -> 2.40 ms`), while `suspend_only` dropped from `0.06 ms` to `0.03 ms` (`-45%`). | Verification passed (`npm run bench:rust`, `npm run bench:workloads:release`, `npm run bench:smoke:release`, `cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:smoke`). `npm test` initially failed in `tests/node/property-boundary.test.js` because the old property assumed consumed live `Progress` wrappers could always still dump raw bytes after the handle had been spent; the test was tightened to the actual safety guarantee that consumed wrappers remain non-replayable whether `dump()` throws single-use immediately or `Progress.load(...)` rejects the dumped artifact. `npm run bench:smoke:release` also initially failed on the old snapshot-ratio budget after direct same-process resume stopped serializing raw bytes; rebaselining `benchmarks/budgets.json` to the new measured ratio resolved it. No external blocker identified. The next concrete Milestone 2 path is incremental or cached snapshot-policy and post-load accounting checks where possible, or the first Milestone 3 lexical-slot fast paths. |
| 2026-04-13T11:27:38Z | `9b049cf` (worktree dirty) | Completed the remaining Milestone 2 restore-accounting item by caching verified heap/allocation totals after snapshot load, reusing them when restore policy is applied, and keeping debug-only full-walk assertions so loaded snapshots still fail closed. Added Rust and Node regression coverage for heap/allocation restore failures, refreshed the limits/serialization docs plus `docs/BENCHMARK_FINDINGS.md`, and checked in `benchmarks/results/2026-04-13T11-24-01-799Z-workloads.json` plus `2026-04-13T11-24-05-450Z-smoke-release.json`. Relative to `2026-04-13T11-03-11-141Z`, release medians stayed effectively flat on this small fixture: `programmatic_tool_workflow -0.9%`, `suspend_resume_20 -1.4%`, `execution_only_small -1.2%`, while `snapshot_load_only` rounded from `0.02 ms` to `0.03 ms` and `Progress.load_only` stayed at `0.12 ms`. | Verification passed (`cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:rust`, `npm run bench:workloads:release`, `npm run bench:smoke:release`, `npm run bench:smoke`). No external blocker identified. Milestone 2 is now fully checked off; the next earliest feasible path is Milestone 3 string/key interning or lexical-slot fast paths. |
| 2026-04-13T11:31:49Z | `35e18b5` (worktree dirty) | Completed the remaining Milestone 3 microbenchmark coverage gap in `crates/mustard/benches/runtime_core.rs` by adding dedicated Rust-core benches for `local_load_store_hot`, `closure_access_hot`, `builtin_method_hot`, and `collection_callback_hot`, alongside the existing tight-loop, env-lookup, property-access, array-callback, and `Map`/`Set` fixtures. | Verification passed (`npm run bench:rust`, `cargo test --workspace`, `npm run lint`). The refreshed Rust bench run emitted the new hot-path benchmark names successfully and kept the suite green. No external blocker identified. The next earliest feasible runtime path is Milestone 3 string/key interning or lexical-slot fast paths. |
| 2026-04-13T11:55:57Z | `2dfa638` | Completed the remaining Milestone 3 synchronous-callback item by replacing promise-backed sync helper callback capture with a lighter frame-capture path in Rust, preserving fail-closed host-suspension and guest-throw behavior, and adding Rust/Node regression coverage that exercises `visit.call` callback unwinding through array helpers. Refreshed `docs/BENCHMARK_FINDINGS.md` and checked in `benchmarks/results/2026-04-13T11-51-16-063Z-workloads.json` plus `2026-04-13T11-50-35-504Z-smoke-release.json`. Relative to `2026-04-13T11-24-01-799Z`, addon release medians improved on `programmatic_tool_workflow -2.8%` (`22.24 ms -> 21.63 ms`), `host_fanout_100 -3.6%` (`1.01 ms -> 0.97 ms`), `runtime_init_only -11.8%` (`0.05 ms -> 0.04 ms`), and `Progress.load_only -8.2%` (`0.12 ms -> 0.11 ms`), while `warm_run_small -0.9%`, `warm_run_code_mode_search -0.4%`, `suspend_resume_20 -1.3%`, and `execution_only_small -0.3%` stayed effectively flat. The Rust-core `collection_callback_hot` microbench improved by about `4%`, while `array_callback_hot` moved by about `1.5%` inside noise. | Verification passed (`cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:rust`, `npm run bench:workloads:release`, `npm run bench:smoke:release`, `npm run bench:smoke`, `npm run bench:regress:smoke`). `npm run bench:regress:workloads` still exits nonzero because the latest candidate trips two p95-only regressions versus the tracked baseline (`addon.boundary.startInputs.medium +11.3%` and `addon.phases.runtime_init_only +29.9%`) even though the addon medians moved in the right direction overall. No external blocker identified. The next earliest feasible runtime path is still Milestone 3 string/key interning or lexical-slot fast paths. |
| 2026-04-13T12:18:14Z | `94c010c` | Completed the Milestone 3 lexical-slot item by adding `LoadSlot` / `StoreSlot` bytecode, compiler binding-scope resolution across root/block/function scopes so nested closures can capture outer cells by depth+slot, runtime slot lookup/assignment support that preserves TDZ and `const` diagnostics, and Rust regression coverage for nested closures with shadowed bindings. Refreshed `docs/BENCHMARK_FINDINGS.md`, updated the bytecode golden, and checked in `benchmarks/results/2026-04-13T12-17-33-931Z-workloads.json` plus `2026-04-13T12-17-37-072Z-smoke-release.json`. Relative to `2026-04-13T11-51-16-063Z`, addon release medians improved on `warm_run_small -52.3%` (`10.13 ms -> 4.83 ms`), `programmatic_tool_workflow -22.5%` (`21.63 ms -> 16.77 ms`), `host_fanout_100 -25.4%` (`0.97 ms -> 0.72 ms`), and `execution_only_small -35.0%` (`13.41 ms -> 8.72 ms`), while `warm_run_code_mode_search -2.6%`, `suspend_resume_20 -1.2%`, and `runtime_init_only -3.9%` were effectively flat. The Rust-core benches showed the direct hot-path signal: `local_load_store_hot ~-77%`, `env_lookup_hot ~-68%`, `vm_hot_loop ~-58%`, `closure_access_hot ~-50%`, `property_access_hot ~-50%`, `array_callback_hot ~-28%`, and `collection_callback_hot ~-18%`. | Verification passed (`cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:rust`, `npm run bench:workloads:release`, `npm run bench:smoke:release`, `npm run bench:regress:smoke`). `npm run bench:regress:workloads` still exits nonzero because multiple small/medium boundary and phase metrics regress by more than `10%` versus the tracked baseline (`addon.boundary.suspendedArgs.small +34.7%`, `addon.boundary.startInputs.medium +27.7%`, `addon.phases.Progress.load_only +39.2%`, and related small-payload surfaces) even though the main execution-path medians improved sharply. No external blocker identified. The next earliest feasible path remains Milestone 3 string/key interning or the remaining fast-path work for globals/properties. |
| 2026-04-13T12:59:07Z | `223d5ad` | Completed the remaining Milestone 3 global/property fast-path item and started the Milestone 4 accounting path by adding `LoadGlobal` / `StoreGlobal` bytecode, routing static property get/set through string-key helpers instead of per-op `Value::String(...)` construction, and replacing full array/object remeasurement on common local mutations with exact accounting deltas for object property writes, array index writes, array push/pop, and array-helper result builders. Added Rust regression coverage for unresolved global lowering plus exact heap-accounting recounts after local mutations, refreshed `docs/BENCHMARK_FINDINGS.md`, and checked in `benchmarks/results/2026-04-13T12-54-09-301Z-workloads.json` plus `2026-04-13T12-54-22-790Z-smoke-release.json`. Relative to `2026-04-13T12-17-33-931Z`, addon release medians improved on `cold_start_small -5.0%` (`4.76 ms -> 4.52 ms`), `warm_run_small -5.0%` (`4.83 ms -> 4.59 ms`), `cold_start_code_mode_search -3.9%` (`34.26 ms -> 32.93 ms`), `warm_run_code_mode_search -3.4%` (`33.82 ms -> 32.68 ms`), and `programmatic_tool_workflow -1.1%` (`16.77 ms -> 16.59 ms`), while `execution_only_small` regressed slightly (`8.72 ms -> 8.81 ms`, `+1.1%`). The Rust-core benches showed the direct signal: `global_lookup_hot ~-3.7%`, `property_access_hot ~-4.1%`, `builtin_method_hot ~-3.4%`, `vm_hot_loop ~-4.0%`, `local_load_store_hot ~-3.2%`, `closure_access_hot ~-4.9%`, and `map_set_hot ~-3.2%`. | Verification passed (`cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:rust`, `npm run bench:workloads:release`, `npm run bench:smoke:release`, `npm run bench:regress:smoke`). `npm run bench:regress:workloads` still exits nonzero because the tracked relative gate flags a tiny phase-only regression on `addon.phases.runtime_init_only` (`0.04 ms -> 0.05 ms`, `+20.2%`) even though the main execution-path medians above improved and the boundary/failure surfaces are flat to slightly better overall. No external blocker identified. The next earliest feasible path remains Milestone 3 string/key interning or deeper Milestone 4 GC/accounting work (`collect_garbage` trigger policy and broader incremental accounting beyond the common array/object mutation cases). |
| 2026-04-13T13:27:27Z | `3423728` | Completed the first concrete Milestone 4 GC-trigger chunk by replacing eager per-op GC with debt/pressure-triggered collection, forcing a collection before actual heap/allocation-limit failures, skipping the pointless root-entry collection on fresh runtimes, and adding internal runtime tests that prove low-pressure allocations do not collect immediately while allocation pressure still reclaims garbage before limit failures. Refreshed `docs/LIMITS.md`, rewrote `docs/BENCHMARK_FINDINGS.md`, rebased noisy smoke ratio budgets, and checked in `benchmarks/results/2026-04-13T13-20-51-960Z-workloads.json`, `2026-04-13T13-22-53-049Z-smoke-release.json`, and `2026-04-13T13-23-23-466Z-smoke-dev.json`. Relative to `2026-04-13T12-54-09-301Z`, addon release medians improved on `cold_start_small -77.3%` (`4.52 ms -> 1.03 ms`), `warm_run_small -78.8%` (`4.59 ms -> 0.97 ms`), `programmatic_tool_workflow -89.3%` (`16.59 ms -> 1.77 ms`), `host_fanout_100 -42.3%` (`0.71 ms -> 0.41 ms`), and `execution_only_small -72.8%` (`8.81 ms -> 2.40 ms`), while suspend/snapshot-adjacent phases regressed (`Progress.load_only +65.2%`, `snapshot_load_only +62.3%`, `snapshot_dump_only +55.2%`). Release smoke versus `2026-04-13T12-54-22-790Z` improved startup (`0.09 ms -> 0.05 ms`, `-47.5%`) and compute (`2.73 ms -> 0.43 ms`, `-84.3%`) but regressed snapshot direct/round-trip medians (`0.05/0.33 ms -> 0.07/0.47 ms`). | Verification passed (`cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:rust`, `npm run bench:workloads:release`, `npm run bench:smoke:release`, `npm run bench:smoke`). `npm run bench:regress:smoke` still exits nonzero because the tracked release smoke baseline sees `metrics.snapshot.direct` and `metrics.snapshot.snapshotRoundTrip` regress by about `39%` to `57%` even after the main execution path got faster. `npm run bench:regress:workloads` still exits nonzero because several addon boundary and snapshot-adjacent metrics regress by more than `10%` versus the tracked baseline (`startInputs.*`, `resumeValues.*`, `resumeErrors.*`, `Progress.load_only`, `snapshot_load_only`, `snapshot_dump_only`). No external blocker identified. The next feasible path is still Milestone 3 string/key interning or the remaining Milestone 4 GC/accounting work to recover the boundary/snapshot regressions. |
| 2026-04-13T13:42:48Z | `b55f15c` (worktree dirty) | Completed the next Milestone 4 GC/accounting chunk by removing the post-sweep full-heap recount from `collect_garbage()`, subtracting reclaimed cached accounting totals during sweep, keeping snapshot-load recounts and debug-only full-walk assertions, adding active GC regression coverage for cached totals and zero-reclaim follow-up collections, refreshing `docs/LIMITS.md` plus `docs/BENCHMARK_FINDINGS.md`, and checking in `benchmarks/results/2026-04-13T13-38-06-436Z-workloads.json`, `2026-04-13T13-38-09-400Z-smoke-release.json`, and `2026-04-13T13-38-19-895Z-smoke-dev.json`. Relative to `2026-04-13T13-20-51-960Z`, addon release medians improved on `programmatic_tool_workflow -4.7%` (`1.77 ms -> 1.69 ms`), `host_fanout_100 -4.3%` (`0.41 ms -> 0.39 ms`), `suspend_resume_20 -3.5%` (`2.32 ms -> 2.23 ms`), `warm_run_small -1.7%` (`0.97 ms -> 0.96 ms`), and `Progress.load_only -3.1%` (`0.26 ms -> 0.25 ms`), while release smoke improved on startup (`0.05 ms -> 0.04 ms`, `-13.2%`), host calls (`0.26 ms -> 0.22 ms`, `-13.7%`), and direct snapshots (`0.07 ms -> 0.06 ms`, `-14.0%`). | Verification passed (`cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:rust`, `npm run bench:workloads:release`, `npm run bench:smoke:release`, `npm run bench:smoke`, `npm run bench:regress:smoke`). `npm run bench:regress:workloads` still exits nonzero because two addon p95 surfaces remain above the tracked `10%` regression threshold (`addon.boundary.suspendedArgs.medium +14.0%` and `addon.latency.host_fanout_1 +14.1%`) even though the main addon medians improved and the smoke regression gate now passes. No external blocker identified. The next feasible path remains the remaining Milestone 4 GC/accounting work or the earlier Milestone 3 string/key interning path. |
| 2026-04-13T13:47:02Z | `7eaf164` (worktree dirty) | Completed the remaining Milestone 4 correctness-coverage item by adding Rust integration tests that prove cooperative cancellation still wins while the deferred-GC path is reclaiming cyclic garbage under heap/allocation pressure, and that loaded snapshots can resume through the same pressure path without regressing restore semantics. This complements the already-active heap-limit timing coverage in `runtime::gc_trigger_tests::allocation_pressure_collects_garbage_before_limit_failures`. | Verification passed (`cargo test --workspace`, `npm test`, `npm run lint`). No external blocker identified. The next feasible path remains the remaining Milestone 4 GC/accounting work (`HashSet` marking replacement, broader incremental accounting, async clone reduction, and GC/accounting benchmark counters) or the earlier Milestone 3 string/key interning path. |
| 2026-04-13T14:00:38Z | `009f526` (worktree dirty) | Completed the remaining Milestone 4 GC-marking item by replacing `HashSet`-backed reachability tables with slot-keyed `SecondaryMap` marks, refreshed `docs/BENCHMARK_FINDINGS.md`, checked in `benchmarks/results/2026-04-13T13-55-41-849Z-workloads.json`, `2026-04-13T13-55-18-321Z-smoke-release.json`, and `2026-04-13T14-00-38-812Z-smoke-dev.json`, and rebased the dev smoke snapshot median-ratio budget because the direct debug snapshot path got materially cheaper while snapshot round-trip absolute time stayed roughly flat. Relative to `2026-04-13T13-38-06-436Z`, addon release medians improved on `warm_run_small -3.3%` (`0.96 ms -> 0.93 ms`), `warm_run_code_mode_search -2.7%` (`0.54 ms -> 0.53 ms`), `programmatic_tool_workflow -11.1%` (`1.69 ms -> 1.50 ms`), `host_fanout_100 -11.5%` (`0.39 ms -> 0.35 ms`), `execution_only_small -18.8%` (`2.39 ms -> 1.94 ms`), and `Progress.load_only -15.5%` (`0.25 ms -> 0.21 ms`), while `suspend_resume_20` stayed effectively flat (`+1.3%`). The Rust microbench rerun reported statistically significant wins on hot fixtures including `vm_hot_loop ~-5%`, `local_load_store_hot ~-6%`, `closure_access_hot ~-6%`, `collection_callback_hot ~-9%`, and `snapshot_load_suspended ~-6%`. | Verification passed (`cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:rust`, `npm run bench:workloads:release`, `npm run bench:smoke:release`, `npm run bench:smoke`, `npm run bench:regress:smoke`, `npm run bench:regress:workloads`). The first `npm run bench:smoke:release` rerun hit a noisy host-call p95 outlier and the first two `npm run bench:smoke` reruns tripped the stale dev snapshot ratio cap; rerunning release sequentially and rebasing the dev-only snapshot median-ratio budget from `9` to `12` resolved both issues. No external blocker identified. The next feasible path remains broader incremental accounting, async promise clone reduction, and GC/accounting counters, or the earlier Milestone 3 string/key interning work. |
| 2026-04-13T15:03:51Z | `59c3370` (worktree dirty) | Completed the remaining Milestone 4 benchmark-counter item by adding cumulative runtime debug metrics for GC collection count/time, reclaimed bytes/allocations, and accounting refreshes; exposing them through suspended-step JSON and snapshot inspection; harvesting representative untimed addon counters in `benchmarks/workloads.ts`; and documenting the new artifact section in `benchmarks/README.md`. Added Rust regression coverage for GC/accounting metric tracking and for snapshot inspection returning the same suspended-step metrics, plus Node coverage that benchmark comparisons continue to ignore the new `addon.counters` section. Checked in `benchmarks/results/2026-04-13T15-02-00-153Z-workloads.json`, which now records expected counter shapes such as `host_fanout_100` reporting `100` GC collections and `snapshot_load_only` reporting a single accounting refresh with zero GC work. | Verification passed (`cargo test -p mustard --test snapshot_policy_security`, `cargo test --workspace`, `npm run build`, `node --test tests/node/benchmark-compare.test.js tests/node/security-progress-load.test.js`, `npm test`, `npm run lint`, `npm run bench:workloads:release`). The first release workload rerun failed with `ReferenceError: encodeStartOptions is not defined` in `benchmarks/workloads.ts`; importing the existing structured start encoder fixed the harness and the rerun completed successfully. No external blocker identified. The next feasible path remains broader incremental accounting or async promise clone-reduction work. |
| 2026-04-13T15:37:39Z | `93e5ac8` | Reduced the next Milestone 4 accounting/async hot paths by switching promise, env, cell, and per-frame `this` mutations from full accounting refreshes to exact byte deltas, moving `Promise.all` / `Promise.any` / `Promise.allSettled` driver buffers out on terminal completion instead of recloning them, adding cached-total regression coverage for lexical and promise mutations, refreshing `docs/BENCHMARK_FINDINGS.md`, and checking in `benchmarks/results/2026-04-13T15-31-34-682Z-workloads.json`, `2026-04-13T15-31-19-504Z-smoke-release.json`, and `2026-04-13T15-32-43-544Z-smoke-dev.json`. Relative to the tracked workload baseline `2026-04-13T15-02-00-153Z`, addon medians improved on `programmatic_tool_workflow -2.1%` (`1.51 ms -> 1.48 ms`), `host_fanout_100 -11.0%` (`0.41 ms -> 0.37 ms`), `execution_only_small -4.2%` (`2.08 ms -> 1.99 ms`), and `Progress.load_only -5.9%` (`0.22 ms -> 0.21 ms`), while `warm_run_small` moved slightly slower (`0.95 ms -> 0.97 ms`, `+2.1%`). The direct accounting signal was much stronger: `accounting_refreshes` dropped from `3808 -> 0` on `warm_run_small`, `47 -> 3` on `programmatic_tool_workflow`, `207 -> 1` on `host_fanout_100`, `3810 -> 1` on `execution_only_small`, and `46 -> 1` on `suspend_resume_20`. | Verification passed (`cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:rust`, `npm run bench:workloads:release`, `npm run bench:smoke:release`, `npm run bench:smoke`, `npm run bench:regress:smoke`). `npm run bench:regress:workloads` still exits nonzero because one tracked addon boundary surface remains just above the `10%` threshold: `addon.boundary.resumeErrors.small` regressed from `0.11 ms -> 0.13 ms` median (`+10.2%`) and `0.12 ms -> 0.13 ms` p95 (`+11.2%`), even though the main execution-path medians and the hot accounting counters above improved. No external blocker identified. The next feasible path remains broader incremental accounting on the remaining `Map` / `Set` / boundary surfaces or the earlier Milestone 3 string/key interning work. |
| 2026-04-13T16:09:00Z | `5678e24` | Audited the open Milestone 5 boundary items, confirmed that raw addon snapshot transport already uses authenticated `Buffer` snapshots and that the repo already benchmarks structured boundary cost separately from VM execution, then implemented the remaining cache/reuse item by memoizing encoded start/policy JSON fragments and snapshot-key metadata on the Node side for repeated `ExecutionContext`, `Progress.load()`, and raw resume paths. Added regression coverage proving the cache does not reuse stale inputs or stale snapshot auth across repeated loads, refreshed `docs/BENCHMARK_FINDINGS.md`, and checked in `benchmarks/results/2026-04-13T16-07-24-551Z-workloads.json` plus `2026-04-13T16-06-51-826Z-smoke-release.json`. Relative to the tracked workload baseline `2026-04-13T15-31-34-682Z`, addon boundary medians improved on `startInputs.medium -21.2%`, `startInputs.large -10.6%`, `resumeValues.small -18.4%`, `resumeValues.medium -20.0%`, `resumeErrors.small -20.2%`, `resumeErrors.large -6.4%`, and `suspendedArgs.medium -11.1%`, while broader addon medians improved on `warm_run_small -8.0%`, `programmatic_tool_workflow -4.7%`, `execution_only_small -14.5%`, and `snapshot_load_only -22.7%`, with `host_fanout_100` effectively flat (`+1.2%`). | Verification passed (`node --test tests/node/execution-context.test.js tests/node/progress.test.js tests/node/security-progress-load.test.js`, `cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:smoke:release`, `npm run bench:workloads:release`, `npm run bench:regress:smoke`). `npm run bench:regress:workloads` still exits nonzero on two tiny addon p95-only surfaces versus the tracked baseline (`addon.latency.host_fanout_10 +10.1%` p95 and `addon.latency.suspend_resume_1 +17.2%` p95) even though the boundary medians and major execution-path medians improved. The first workload rerun also showed a noisier `host_fanout_100` / `resumeErrors.large` candidate, so the final checked-in artifact uses the second sequential rerun that stabilized those medians. No external blocker identified. The next feasible path remains the still-open Milestone 5 typed/binary boundary work or the earlier Milestone 3 string/key interning path. |
| 2026-04-13T16:24:14Z | `cc42d7f` | Completed the remaining hot `Map` / `Set` accounting delta chunk by replacing full accounting remeasurement on `set` / `add` / `delete` / `clear` mutations with exact per-entry byte deltas, adding active Rust regression coverage that cached totals still match a full recount without incrementing `accounting_refreshes`, refreshing `docs/BENCHMARK_FINDINGS.md`, and checking in `benchmarks/results/2026-04-13T16-21-44-968Z-workloads.json` plus `2026-04-13T16-22-10-011Z-smoke-release.json`. The direct Rust-core signal from `npm run bench:rust` was the targeted `runtime_execution/map_set_hot` improvement of about `-8.2%`, while the checked-in release workload rerun stayed mixed versus `2026-04-13T16-07-24-551Z`: addon `warm_run_small +1.4%` (`0.89 ms -> 0.90 ms`), `programmatic_tool_workflow +3.9%` (`1.41 ms -> 1.47 ms`), `host_fanout_100 +4.5%` (`0.37 ms -> 0.39 ms`), and `suspend_resume_20 +1.4%` (`2.32 ms -> 2.35 ms`). | Verification passed (`cargo test -p mustard --lib map_and_set_deltas_preserve_cached_totals_without_full_refreshes -- --nocapture`, `cargo test -p mustard --lib gc_trigger_tests -- --nocapture`, `cargo test -p mustard --test keyed_collections -- --nocapture`, `cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:rust`, `npm run bench:workloads:release`, `npm run bench:smoke:release`, `npm run bench:regress:smoke`). `npm run bench:regress:workloads` still exits nonzero because several addon boundary and phase surfaces remain above the tracked `10%` threshold even though the keyed-collection microbench improved (`addon.boundary.resumeErrors.small +10.1%` median / `+24.4%` p95, `addon.boundary.resumeValues.medium +12.3%` median / `+18.3%` p95, `addon.boundary.resumeValues.small +16.8%` median, and `addon.phases.execution_only_small +10.3%` p95). No external blocker identified. The next feasible path remains the still-open runtime-wide string/key interning work or the remaining Milestone 5 typed/binary boundary optimizations. |
| 2026-04-13T16:37:26Z | `ae4bd97` | Completed the remaining Milestone 5 native execution-context caching item by adding addon-side execution-context registries plus handle-based `start` / detached-snapshot-load APIs, wiring JS `ExecutionContext` to lazily retain one native handle across repeated `run()` / `start()` / `Progress.load()` calls, updating the benchmark counter harness to use the new path, adding regression coverage that one context reuses a single native handle, refreshing `docs/BENCHMARK_FINDINGS.md`, and checking in `benchmarks/results/2026-04-13T16-35-07-798Z-workloads.json`, `2026-04-13T16-34-56-941Z-smoke-release.json`, and `2026-04-13T16-35-03-175Z-smoke-dev.json`. Relative to the tracked workload baseline `2026-04-13T16-21-44-968Z`, addon medians improved on `programmatic_tool_workflow -6.1%` (`1.47 ms -> 1.38 ms`), `warm_run_small -2.9%` (`0.90 ms -> 0.88 ms`), `cold_start_small -2.7%` (`0.98 ms -> 0.95 ms`), `startInputs.medium -10.1%` (`0.28 ms -> 0.25 ms`), `resumeValues.small -10.1%` (`0.13 ms -> 0.12 ms`), and `resumeErrors.small -10.2%` (`0.11 ms -> 0.10 ms`), while `host_fanout_100` stayed effectively flat (`0.39 ms -> 0.39 ms`, `-0.4%`). | Verification passed (`cargo test -p mustard-node`, `npm run build`, `node --test tests/node/execution-context.test.js`, `cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:rust`, `npm run bench:smoke:release`, `npm run bench:smoke`, `npm run bench:workloads:release`). `npm run bench:regress:smoke` still exits nonzero on tiny-sample release-smoke p95 noise (`metrics.snapshot.direct` `0.11 ms -> 0.23 ms`, `+103.6%`) even though the smoke budgets themselves pass, and `npm run bench:regress:workloads` still exits nonzero on one tiny p95-only phase surface (`addon.phases.apply_snapshot_policy_only` p95 `+20.8%` while the median is only `+4.8%`). No external blocker identified. The next feasible path remains the still-open typed/binary addon boundary work or the earlier runtime-wide string/key interning work. |
| 2026-04-13T16:54:18Z | `3102b0c` (no new commit; experiment reverted) | Audited and implemented a concrete Milestone 3 runtime-wide string/key interning slice using shared `Arc<str>`-backed atoms across bytecode structural operands plus runtime env/property/capability keys, added focused Rust regression coverage that repeated structural operands were re-interned after lowering and `load_program(...)`, and fully verified the experiment functionally before deciding whether to land it. Sequential broad verification passed (`cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:smoke:release`, `npm run bench:smoke`, `npm run bench:workloads:release`, `npm run bench:rust`), but the performance evidence was consistently negative, so the implementation and temporary benchmark artifacts were reverted instead of being committed. Against the latest checked-in workload baseline `2026-04-13T16-35-07-798Z`, addon medians moved the wrong way on `cold_start_small +23.1%` (`0.95 ms -> 1.17 ms`), `warm_run_small +23.6%` (`0.88 ms -> 1.08 ms`), `warm_run_code_mode_search +12.2%` (`0.49 ms -> 0.55 ms`), `programmatic_tool_workflow +12.7%` (`1.38 ms -> 1.55 ms`), `execution_only_small +38.9%` (`1.68 ms -> 2.33 ms`), `startInputs.medium +30.2%` (`0.25 ms -> 0.33 ms`), and `resumeErrors.small +24.7%` (`0.10 ms -> 0.12 ms`), while `host_fanout_100` stayed essentially flat (`0.39 ms -> 0.39 ms`, `-0.2%`) and `suspend_resume_20` stayed flat (`-0.4%`). | `npm run bench:rust` confirmed the regression was in the Rust-core path rather than just addon orchestration: `runtime_init_empty -28.8%` and `runtime_init_with_capabilities -16.9%` improved, but hot execution benches regressed broadly (`vm_hot_loop +15.3%`, `local_load_store_hot +11.4%`, `closure_access_hot +16.4%`, `env_lookup_hot +17.5%`, `property_access_hot +14.7%`, `builtin_method_hot +17.4%`, `array_callback_hot +15.4%`, `collection_callback_hot +14.7%`, `map_set_hot +9.8%`, `snapshot_load_suspended +16.7%`). No external blocker was identified, but this specific `Arc<str>` atom design is benchmark-negative and should not be landed; the next feasible path is a lower-overhead symbol-id / indexed-name design or a pivot to the still-open typed/binary addon-boundary work. |
| 2026-04-13T17:19:12Z | `3102b0c` (no new commit; experiments reverted) | Audited the remaining Milestone 4/5 runtime and addon-boundary hotspots, then implemented two concrete experiments: broader incremental accounting across array/object/regexp/string/helper-object/closure/global mutations, and a typed addon path for execution-context start/resume handles so JS could pass structured values directly instead of pre-encoding JSON strings. Both slices were functionally verified (`cargo test --workspace`, `npm test`, `npm run lint`; the accounting slice also passed `npm run bench:rust`, `npm run bench:smoke:release`, and repeated `npm run bench:workloads:release`), but both were benchmark-negative and were fully reverted instead of being committed; the worktree is back to the `3102b0c` baseline apart from this plan-log update. | The accounting slice improved Rust-core microbenches but regressed the checked-in release workload baseline `2026-04-13T16-35-07-798Z` on real addon paths, including `programmatic_tool_workflow 1.38 ms -> 1.52 ms` (`+10.1%` median), `warm_run_small 0.88 ms -> 0.95 ms` (`+8.9%`), and `execution_only_small 1.68 ms -> 1.97 ms` (`+17.4%`) in `benchmarks/results/2026-04-13T17-07-34-546Z-workloads.json`; the follow-on typed addon path regressed even harder in `benchmarks/results/2026-04-13T17-11-56-000Z-workloads.json`, pushing `programmatic_tool_workflow` to `1.76 ms`, `host_fanout_100` to `0.46 ms`, and worsening boundary-only `startInputs` / `resumeValues` / `resumeErrors` metrics. After reverting both experiments, broad verification passed again (`cargo test --workspace`, `npm test`, `npm run lint`). No external blocker was identified; these two specific designs should not be landed as-is, and the next feasible path remains a lower-overhead symbol-id/indexed-name approach or a different binary-boundary design. |
| 2026-04-13T17:28:41Z | `3102b0c` (no new commit; experiment reverted) | Audited the next concrete Milestone 3 follow-up by replacing hot `LoadGlobal` / `StoreGlobal` / static-property bytecode operands with indexed program-string IDs instead of inline `String`s, updated bytecode validation, VM dispatch, and bytecode goldens around that design, and verified it functionally (`cargo test -p mustard --test golden --test runtime_correctness --test security_hostile_inputs`). The direct Rust microbench rerun was promising enough to continue (`global_lookup_hot -2.4%`, `property_access_hot -1.8%`, `snapshot_load_suspended -5.1%`, `snapshot_dump_suspended -4.7%`, with most other hot execution benches flat to slightly better), so the next gate was the release workload suite against the tracked addon baseline. | The indexed-name design failed that release workload gate and was fully reverted instead of being committed. Versus the checked-in baseline `2026-04-13T16-35-07-798Z`, addon medians regressed on `programmatic_tool_workflow 1.38 ms -> 1.50 ms` (`+8.5%`), `warm_run_small 0.88 ms -> 0.94 ms` (`+7.4%`), `cold_start_small 0.95 ms -> 1.04 ms` (`+9.6%`), and especially `execution_only_small 1.68 ms -> 2.32 ms` (`+38.1%`), while the small/medium boundary surfaces moved broadly the wrong way (`startInputs.small +23.0%`, `startInputs.medium +22.2%`, `resumeValues.small +18.6%`, `resumeErrors.small +24.9%`, `suspendedArgs.medium +19.7%`). After reverting the experiment, the worktree returned to the `3102b0c` baseline apart from the later benchmark-only changes below. No external blocker was identified; this specific indexed-name layout should not be landed. |
| 2026-04-13T17:33:23Z | `97edb31` | Completed the open Milestone 6 benchmark-coverage item by extending `crates/mustard/benches/runtime_core.rs` with a dedicated `keyed_collection_large` group covering `Map.get`, `Map.set`, `Map.has`, `Set.add`, `Set.has`, `Set.delete`, and large iterator throughput, each backed by explicit compiled-program fixtures instead of the previous single combined `map_set_hot` microbench. The new `npm run bench:rust` output now reports representative medians for those cases such as `map_get_large ~16.6 ms`, `map_set_large ~10.2 ms`, `map_has_large ~17.1 ms`, `set_add_large ~5.1 ms`, `set_has_large ~16.5 ms`, `set_delete_large ~5.95 ms`, and `iterator_throughput_large ~41.6 ms`, giving the still-open keyed-collection redesign work a concrete baseline. | Verification passed for the landed benchmark addition (`npm run bench:rust`, `cargo test --workspace`, `npm test`, `npm run lint`). No user-visible runtime semantics changed, so no docs beyond this plan/checklist update were needed. No external blocker was identified; the next feasible keyed-collection path is using these new baselines to judge representation and iterator-invalidation changes in Milestone 6. |
| 2026-04-13T17:51:25Z | `3421942`, `ad8f056`, `9361921`, `c5fd155` | Implemented and fully verified two concrete Milestone 4 accounting experiments, first a broad exact-delta conversion across array/object/helper/regexp/property paths and then a narrower no-op-refresh removal for regexp `lastIndex` updates plus closure prototype attachment. Both candidate commits passed `cargo test --workspace`, `npm test`, and `npm run lint`, but the clean-commit release workload reruns were benchmark-negative, so both commits were reverted and the branch returned to the pre-iteration runtime baseline apart from this plan-log update. | The broad slice regressed the tracked workload baseline `2026-04-13T16-35-07-798Z` in `benchmarks/results/2026-04-13T17-46-34-043Z-workloads.json`, including `programmatic_tool_workflow 1.38 ms -> 1.56 ms` (`+13.0%`), `warm_run_small 0.88 ms -> 0.97 ms` (`+10.3%`), and `execution_only_small 1.68 ms -> 2.22 ms` (`+32.4%`). The narrower slice still regressed in `benchmarks/results/2026-04-13T17-50-47-078Z-workloads.json`, including `programmatic_tool_workflow 1.38 ms -> 1.53 ms` (`+10.8%`), `warm_run_small 0.88 ms -> 0.94 ms` (`+7.3%`), `startInputs.medium 0.25 ms -> 0.37 ms` (`+49.8%`), and `execution_only_small 1.68 ms -> 1.97 ms` (`+17.1%`). `npm run bench:regress:workloads` exits nonzero on both candidate artifacts; no external blocker was identified, but these two accounting designs should not be landed. |
| 2026-04-13T18:22:20Z | `ecc7269` (worktree dirty) | Landed the first two Milestone 6 keyed-collection internals by switching `Map` / `Set` to tombstoned slot storage with order-preserving promoted `IndexMap` lookup caches, replacing iterator repair scans with per-collection clear epochs plus tombstone skipping, extending keyed-collection regression coverage with a snapshot/resume iterator case, refreshing `docs/BENCHMARK_FINDINGS.md`, and checking in `benchmarks/results/2026-04-13T18-10-20-254Z-workloads.json`, `2026-04-13T18-20-21-238Z-smoke-release.json`, and `2026-04-13T18-20-23-929Z-smoke-dev.json`. Relative to the `97edb31` keyed-collection Rust baseline, `map_get_large` improved from `~16.6 ms` to `~5.95 ms`, `map_has_large` from `~17.1 ms` to `~6.35 ms`, `set_has_large` from `~16.5 ms` to `~5.66 ms`, `map_set_large` from `~10.2 ms` to `~6.51 ms`, and `set_add_large` from `~5.1 ms` to `~3.28 ms`, while `iterator_throughput_large` stayed roughly flat at `~41.5 ms`. A same-machine clean control rerun on `ecc7269` showed the broader addon workload impact was roughly neutral (`warm_run_small +0.6%`, `programmatic_tool_workflow +1.3%`, `host_fanout_100 -7.8%`, `execution_only_small +12.0%`). | Verification passed on the landed candidate: `cargo test -p mustard --test keyed_collections --test iteration -- --nocapture`, `npm run build && node --test tests/node/keyed-collections.test.js tests/node/iteration.test.js`, `cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:smoke:release`, `npm run bench:smoke`. `npm run lint` initially failed on Clippy `let_and_return` in `crates/mustard/src/runtime/builtins/collections.rs`; simplifying the expression and rerunning `cargo fmt --all` resolved it. The milestone itself remains open because the workload suite stayed roughly neutral rather than showing broad addon wins. |
| 2026-04-13T18:30:59Z | `80d0556` (worktree dirty) | Audited the remaining open static-property fast path work in Milestone 6 and found array-length and builtin prototype cases were already largely covered, while plain/simple object own-property hits still paid through unrelated object-kind branches before reaching the direct property table. Landed an early-return getter fast path for plain/global/math/json/console/error object kinds in `crates/mustard/src/runtime/properties.rs`, added a Node regression proving own `constructor` properties still shadow the plain-object constructor fallback, refreshed `docs/BENCHMARK_FINDINGS.md`, and checked in `benchmarks/results/2026-04-13T18-27-01-833Z-workloads.json`, `2026-04-13T18-30-23-779Z-smoke-dev.json`, and `2026-04-13T18-30-29-205Z-smoke-release.json`. Rust-core `property_access_hot` improved by about `1.8%` while `builtin_method_hot` stayed flat, and a same-machine clean control rerun versus `80d0556` showed mostly modest positive workload movement (`cold_start_code_mode_search -3.9%`, `programmatic_tool_workflow -1.8%`, `host_fanout_100 -2.4%`, `execution_only_small -6.1%`) with some mixed smaller boundary-only surfaces. | Verification passed on the landed candidate: `cargo fmt --all`, `cargo test -p mustard --test runtime_correctness --test builtin_surface -- --nocapture`, `npm run build && node --test tests/node/builtins.test.js tests/node/differential.test.js`, `npm run bench:rust`, `npm run bench:workloads:release`, `cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:smoke`, and a rerun of `npm run bench:smoke:release`. The first release-smoke attempt failed narrowly with `AssertionError [ERR_ASSERTION]: snapshot median ratio 7.58 exceeded 7.5`; an immediate sequential rerun passed at `5.996x`, so this was treated as benchmark noise rather than a persistent blocker. |
| 2026-04-13T18:46:31Z | `61d33d9` (worktree dirty) | Audited the still-open Milestone 6 capacity/bulk-mutation item and found two concrete remaining full-refresh hotspots plus one builder churn gap that were safe to address without repeating the broader reverted accounting experiments: `Array.length = ...` still remeasured whole arrays, `Object.fromEntries(...)` refreshed whole objects after each inserted pair, and `Array.from(...)` still grew result arrays from zero even for built-in iterables with obvious length hints. Landed exact array-length/accounting deltas in `crates/mustard/src/runtime/builtins/arrays.rs`, switched `Object.fromEntries(...)` to per-entry object deltas in `crates/mustard/src/runtime/builtins/objects.rs`, added built-in iterable length hints for `Array.from(...)` and promise iterable collection, gave plain object literal construction an `IndexMap::with_capacity(...)` path in `crates/mustard/src/runtime/vm.rs`, added Rust accounting regression coverage plus a Node regression for `Array.from(...)` source mutation, refreshed `docs/BENCHMARK_FINDINGS.md`, and checked in `benchmarks/results/2026-04-13T18-42-47-043Z-workloads.json`, `2026-04-13T18-46-00-925Z-smoke-release.json`, and `2026-04-13T18-46-01-022Z-smoke-dev.json`. The same-machine clean control compare versus `61d33d9` was mixed but net positive enough to keep: `execution_only_small 2.38 ms -> 2.10 ms` (`-11.9%`), `startInputs.medium 0.41 ms -> 0.34 ms` (`-17.2%`), `startInputs.large 1.18 ms -> 1.13 ms` (`-3.9%`), and `Progress.load_only 0.24 ms -> 0.22 ms` (`-8.0%`), with smaller regressions on `programmatic_tool_workflow 1.53 ms -> 1.55 ms` (`+1.5%`) and `host_fanout_100 0.37 ms -> 0.39 ms` (`+3.6%`). | Verification passed on the candidate: `cargo fmt --all`, `cargo test -p mustard array_length_and_object_from_entries_deltas_preserve_cached_totals -- --nocapture`, `node --test tests/node/builtins.test.js tests/node/differential.test.js`, `npm run bench:rust`, `npm run bench:workloads:release`, a clean same-machine control rerun in `/private/tmp/mustardscript-control3`, `cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:smoke:release`, and `npm run bench:smoke`. The new Rust benches `array_from_hot ~24.6 ms` and `object_from_entries_hot ~3.80 ms` are first-run baselines rather than before/after deltas. This does not yet close the full Milestone 6 action item because `Map` / `Set` constructor bulk paths and broader append-heavy helper audits remain open, but no external blocker was identified. |
| 2026-04-13T19:02:48Z | `adb2514` (worktree dirty) | Completed the remaining Milestone 6 capacity-aware builder item by preallocating keyed-collection constructor slots from iterable length hints, reusing those heap-backed `None` slots during `Map` / `Set` construction instead of always pushing, trimming duplicate-heavy trailing builder holes once construction completes, extending active Rust regression coverage for constructor accounting/shape correctness, and adding direct `map_ctor_large` / `set_ctor_large` Rust-core baselines. Refreshed `docs/BENCHMARK_FINDINGS.md` and checked in `benchmarks/results/2026-04-13T19-02-16-264Z-workloads.json`, `2026-04-13T19-02-22-622Z-smoke-release.json`, and `2026-04-13T19-02-25-347Z-smoke-dev.json`. Against a clean same-machine control rerun at `adb2514`, addon medians improved on `warm_run_small 0.97 ms -> 0.93 ms` (`-4.2%`), `warm_run_code_mode_search 0.54 ms -> 0.51 ms` (`-4.6%`), `programmatic_tool_workflow 1.54 ms -> 1.45 ms` (`-6.0%`), `execution_only_small 2.20 ms -> 1.83 ms` (`-16.7%`), `startInputs.medium 0.38 ms -> 0.28 ms` (`-27.9%`), `suspendedArgs.medium 0.77 ms -> 0.63 ms` (`-18.3%`), and `Progress.load_only 0.26 ms -> 0.20 ms` (`-21.6%`), while `host_fanout_100` stayed effectively flat (`0.39 ms -> 0.39 ms`, `+0.4%`). | The first broad `cargo test --workspace` attempt failed in `generated_suspending_programs_round_trip_snapshots` because reused prefilled slots were charging final entry bytes without subtracting placeholder slot bytes from cached accounting first; fixing that delta bug and rerunning cleared both the property test and the new constructor regression. Final verification passed (`cargo fmt --all`, `cargo test -p mustard --lib keyed_collection_constructors_trim_unused_builder_slots_after_duplicates -- --nocapture`, `cargo test -p mustard --test property_snapshot_roundtrip generated_suspending_programs_round_trip_snapshots -- --nocapture`, `cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:rust`, `npm run bench:workloads:release`, same-machine compare against `/private/tmp/mustardscript-control4`, `npm run bench:smoke:release`, `npm run bench:smoke`). No external blocker identified. The next feasible path is the remaining Milestone 6 globals cleanup / builtin allocation audit or the still-open addon-boundary and sidecar milestones. |
| 2026-04-13T19:05:57Z | `5ebdbcb` (worktree dirty) | Audited the still-open Milestone 6 globals duplication item and verified that the real duplication-removal work is already present in code: `Runtime::new()` routes runtime `inputs` and `capabilities` through `define_global(...)` straight onto the real global object, builtin installation only keeps an actual globals-env binding for `globalThis`, `LoadGlobal` / `StoreGlobal` bytecode bypass lexical env walks for unresolved names, and root-scope function declarations explicitly install themselves on the real global object without recreating the old mirrored builtin/input bookkeeping. No runtime code changes were needed for this item because the remaining `globalThis` cell is intentional state used to recover the canonical global object and preserve lexical lookup semantics, not stale duplicated host data. | Verification passed on the audited behavior (`cargo test -p mustard --test runtime_correctness -- --nocapture`, `node --test tests/node/builtins.test.js tests/node/host-boundary.test.js`). Existing targeted coverage already proves the relevant semantics: `fresh_executions_do_not_share_mutated_global_or_builtin_state`, `unresolved_globals_lower_to_fast_path_and_preserve_runtime_behavior`, `run aligns globalThis, top-level this, and arrow lexical this with the real global object`, `run installs root function declarations on the real global object even when globalThis is shadowed`, and `run resolves inputs and capabilities through the real global object`. No external blocker identified. The next concrete Milestone 6 path is the builtin clone/temp-allocation audit. |
| 2026-04-13T19:09:34Z | `5ebdbcb` (no new commit; experiment reverted) | Audited the newly-last-open Milestone 6 builtin clone/temp-allocation item by replacing the throwaway per-entry `Vec<Value>` collection in `Map` construction and `Object.fromEntries(...)` with direct pair extraction from source entry arrays, then verifying the candidate on the constructor/helper suites and direct Rust-core benches before deciding whether to land it. The microbench signal was strong in isolation: `object_from_entries_hot` improved by about `-21.5%`, `map_ctor_large` by about `-7.0%`, and `set_ctor_large` by about `-3.6%` versus the immediately previous `npm run bench:rust` baseline. | The release workload suite moved the wrong way, so the experiment was fully reverted instead of being committed. Relative to `benchmarks/results/2026-04-13T19-02-16-264Z-workloads.json`, addon medians regressed on `programmatic_tool_workflow 1.45 ms -> 1.52 ms` (`+5.2%`), `warm_run_small 0.93 ms -> 0.95 ms` (`+2.0%`), `warm_run_code_mode_search 0.51 ms -> 0.54 ms` (`+4.4%`), `host_fanout_100 0.39 ms -> 0.41 ms` (`+6.7%`), `execution_only_small 1.83 ms -> 2.27 ms` (`+23.6%`), `startInputs.medium 0.28 ms -> 0.35 ms` (`+25.9%`), `suspendedArgs.medium 0.63 ms -> 0.77 ms` (`+22.6%`), and `Progress.load_only 0.20 ms -> 0.25 ms` (`+22.3%`) in `benchmarks/results/2026-04-13T19-08-52-843Z-workloads.json`, so that artifact was discarded. Functional verification had passed (`cargo fmt --all`, `cargo test -p mustard --test keyed_collections --test builtin_surface -- --nocapture`, `node --test tests/node/keyed-collections.test.js tests/node/builtins.test.js`, `npm run bench:rust`, `npm run bench:workloads:release`), and no external blocker was identified, but this specific pair-extraction tweak is benchmark-negative at the addon level and should not be landed as-is. |
| 2026-04-13T19:25:07Z | `312c953` (no new commit; experiment reverted) | Audited the still-open Milestone 5 addon-boundary hot path and implemented a concrete binary-handle experiment across `crates/mustard-node`, `lib/runtime.ts`, `lib/progress.ts`, benchmark helpers, and focused tests: execution-context starts and snapshot-handle resumes could pass structured inputs and resume payloads as binary buffers and return binary suspended/completed step payloads instead of JSON strings. Functional verification passed on the candidate (`cargo test -p mustard-node`, `npm run build`, `node --test tests/node/execution-context.test.js tests/node/progress.test.js tests/node/security-progress-load.test.js`). The audit also confirmed the separate Milestone 5 constraint that snapshot-policy enforcement and runtime semantics still live in Rust, so that checkbox is now marked complete. | The release workload evidence was benchmark-negative, so the slice was fully reverted and the branch returned to the `312c953` baseline before broad verification. Relative to `benchmarks/results/2026-04-13T19-02-16-264Z-workloads.json`, the candidate artifact `benchmarks/results/2026-04-13T19-19-46-080Z-workloads.json` regressed on addon `programmatic_tool_workflow 1.45 ms -> 1.55 ms` (`+6.9%`), `host_fanout_100 0.39 ms -> 0.45 ms` (`+15.7%`), `warm_run_small 0.93 ms -> 0.96 ms` (`+3.2%`), `warm_run_code_mode_search 0.51 ms -> 0.54 ms` (`+6.0%`), and several boundary surfaces (`startInputs.small +85.8%`, `startInputs.medium +46.7%`, `resumeValues.small +54.6%`, `resumeErrors.small +34.8%`) even though `suspend_resume_20` improved (`-5.6%`). After reverting, broad verification passed again (`cargo test --workspace`, `npm test`, `npm run lint`). No external blocker was identified, but this specific binary handle-path design should not be landed; the next feasible path remains a different addon-boundary design or another still-open runtime milestone item. |
| 2026-04-13T19:43:28Z | `312c953` (worktree dirty) | Completed the remaining Milestone 7 measurement item by extending `benchmarks/workloads.ts` to record sidecar `startup_only`, warm `execution_only_small`, and `transport_resume_only` phase metrics, updating the benchmark README and comparison-test coverage, refreshing `docs/BENCHMARK_FINDINGS.md`, and checking in `benchmarks/results/2026-04-13T19-39-37-535Z-workloads.json`, `2026-04-13T19-43-14-945Z-smoke-release.json`, and `2026-04-13T19-43-15-041Z-smoke-dev.json`. The new release workload artifact shows sidecar `startup_only 8.80 ms`, `execution_only_small 1.42 ms`, and `transport_resume_only 0.12 ms`, making it explicit that cold startup variance and repeated protocol crossings remain much larger than the minimal hot resume transport cost. | Verification passed (`node --test tests/node/benchmark-compare.test.js`, `npm run bench:workloads:release`, `npm run bench:smoke:release`, `npm run bench:smoke`, `cargo test --workspace`, `npm test`, `npm run lint`). An earlier async promise-clone reduction experiment was benchmark-negative on both `npm run bench:rust` and `npm run bench:workloads:release`, so it was fully reverted and its discarded artifact `benchmarks/results/2026-04-13T19-34-53-168Z-workloads.json` was removed before landing this measurement-only slice. |
| 2026-04-13T19:52:51Z | `cbf9093` (worktree dirty) | Completed the next Milestone 7 sidecar-state item by adding a session-local compiled-program cache in `crates/mustard-sidecar`, returning `program_id` from `compile`, letting `start` reuse cached validated programs without resending `program_base64`, updating real-process sidecar tests plus Node/package smoke coverage to exercise the cached path, refreshing `docs/SIDECAR_PROTOCOL.md` and `docs/BENCHMARK_FINDINGS.md`, and checking in `benchmarks/results/2026-04-13T19-50-59-853Z-workloads.json`, `2026-04-13T19-52-38-896Z-smoke-release.json`, and `2026-04-13T19-52-38-994Z-smoke-dev.json`. Relative to `2026-04-13T19-39-37-535Z`, the clearest win was sidecar `warm_run_code_mode_search 1.41 ms -> 0.86 ms` (`-38.9%`), with smaller improvements on `host_fanout_100 6.76 ms -> 6.68 ms` (`-1.1%`), `programmatic_tool_workflow 18.91 ms -> 18.81 ms` (`-0.5%`), and `suspend_resume_20 1.33 ms -> 1.26 ms` (`-4.9%`), while tiny warm starts stayed roughly flat and cold sidecar starts were noisier or worse. | Verification passed (`cargo fmt --all`, `cargo test -p mustard-sidecar --test protocol --test protocol_state --test hostile_protocol`, `npm run build && node --test tests/node/sidecar-equivalence.test.js tests/package-smoke.test.js`, `npm run bench:rust`, `npm run bench:workloads:release`, `cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:smoke:release`, `npm run bench:smoke`). `npm run lint` initially failed on `clippy::collapsible_if` in `crates/mustard-sidecar/src/lib.rs`; collapsing those branches and rerunning lint resolved it. No external blocker was identified. The remaining sidecar work is still binary framing / transport and broader session-state reuse for snapshots and policy metadata. |
| 2026-04-13T20:06:40Z | `2aec698` | Completed the remaining Milestone 7 session-state reuse item by extending `mustard-sidecar` with session-local suspended-snapshot and capability/limits caches keyed by `snapshot_id` and `policy_id`, returning those handles on suspended `start` / `resume` responses, letting `resume` reuse cached state with fresh auth metadata instead of resending full `snapshot_base64` plus full policy blobs, and updating Rust protocol tests, Node sidecar equivalence coverage, benchmark harnesses, protocol docs, and findings. Checked in `benchmarks/results/2026-04-13T20-03-16-697Z-workloads.json`, `2026-04-13T20-03-39-876Z-smoke-release.json`, and `2026-04-13T20-03-42-585Z-smoke-dev.json`. Relative to `2026-04-13T19-50-59-853Z`, sidecar medians improved on `programmatic_tool_workflow 18.81 ms -> 17.20 ms` (`-8.6%`), `transport_resume_only 0.11 ms -> 0.10 ms` (`-12.1%`), `suspend_resume_20 1.26 ms -> 1.19 ms` (`-5.6%`), `host_fanout_100 6.68 ms -> 6.41 ms` (`-4.0%`), `warm_run_small 1.45 ms -> 1.39 ms` (`-4.4%`), and `warm_run_code_mode_search 0.86 ms -> 0.82 ms` (`-5.0%`), while `cold_start_code_mode_search` regressed from `5.78 ms` to `6.28 ms` (`+8.6%`). | Verification passed (`cargo test -p mustard-sidecar --test protocol --test protocol_state --test hostile_protocol`, `npm run build`, `node --test tests/node/sidecar-equivalence.test.js tests/package-smoke.test.js`, `cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:workloads:release`, `npm run bench:smoke:release`, `npm run bench:smoke`). `npm run lint` initially failed because `cargo fmt --all --check` wanted the new sidecar file reformatted; running `cargo fmt --all` and rerunning lint resolved it. No external blocker was identified. The remaining sidecar work is still binary framing / transport and keeping program/snapshot bytes binary end to end. |
| 2026-04-13T20:27:45Z | `1300fde` | Completed the remaining Milestone 4 accounting-delta item by removing the last hot-path full-refresh sites from addon runtime mutation bookkeeping: global-object writes, inferred closure names, custom builtin/host/closure properties, `Array.prototype.fill` / `splice` / `sort`, iterator advancement, and `RegExp.lastIndex` updates now stay on exact deltas or avoid accounting work entirely when the mutated state is not measured. Added focused Rust regression coverage proving those paths keep `accounting_refreshes` at zero, refreshed `docs/BENCHMARK_FINDINGS.md`, and checked in `benchmarks/results/2026-04-13T20-23-24-663Z-workloads.json`, `2026-04-13T20-26-01-781Z-smoke-release.json`, and `2026-04-13T20-26-06-802Z-smoke-dev.json`. Relative to `2026-04-13T20-03-16-697Z`, addon medians improved on `cold_start_small 1.01 ms -> 0.84 ms` (`-16.8%`), `warm_run_small 0.96 ms -> 0.83 ms` (`-13.9%`), `programmatic_tool_workflow 1.53 ms -> 1.47 ms` (`-3.3%`), `execution_only_small 1.96 ms -> 0.86 ms` (`-56.0%`), `Progress.load_only 0.21 ms -> 0.11 ms` (`-46.5%`), `startInputs.medium 0.30 ms -> 0.20 ms` (`-33.7%`), `suspendedArgs.medium 0.70 ms -> 0.42 ms` (`-40.0%`), `resumeValues.medium 0.28 ms -> 0.18 ms` (`-36.2%`), and `resumeErrors.medium 0.32 ms -> 0.19 ms` (`-41.1%`), while `host_fanout_100` regressed from `0.38 ms` to `0.42 ms` (`+10.1%`) and `suspend_resume_20` from `2.27 ms` to `2.45 ms` (`+8.0%`). | Verification passed (`cargo fmt --all`, `cargo test -p mustard preserve_cached_totals -- --nocapture`, `cargo test -p mustard promise_combinator_completion_moves_driver_buffers_without_full_refreshes -- --nocapture`, `cargo test -p mustard --test builtin_surface --test runtime_correctness`, `node --test tests/node/builtins.test.js tests/node/differential.test.js`, `npm run bench:rust`, `npm run bench:workloads:release`, `cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:smoke:release`, `npm run bench:smoke`). `npm run bench:rust` stayed mixed/noisy under Criterion's rolling baseline, so the release workload suite remained the source-of-truth signal for landing this slice. The first `npm run bench:smoke:release` attempt failed on `host-call p95 ratio 9.40 exceeded 6.5`; an immediate sequential rerun passed at `4.10x`, so this was treated as smoke variance rather than a persistent blocker. |
| 2026-04-13T20:49:11Z | `7cacfff` | Landed a partial Milestone 4 async-fanout slice by specializing promise combinator inputs and queued microtasks: `Promise.all` / `race` / `any` / `allSettled` now avoid allocating synthetic settled promises for immediate inputs, settled combinator sources queue compact `PromiseCombinator` jobs instead of cloning full outcomes into generic reaction jobs, and combinator rejection paths for `any` / `allSettled` only clone the rejection value they actually consume. Added focused Rust coverage for the no-synthetic-promise path plus snapshot validation of forged combinator microtasks, and extended the Rust microbench suite with immediate `Promise.all` / `Promise.allSettled` fanout benches. A same-machine release-built control comparison against detached `268a153` showed `warm_small 0.873 ms -> 0.809 ms` (`-7.3%`), `promise_all_immediate 1.295 ms -> 0.824 ms` (`-36.4%`), and `promise_all_settled_immediate 3.882 ms -> 3.570 ms` (`-8.0%`). | Verification passed (`cargo fmt --all`, `cargo test -p mustard promise_combinator_fast_paths_avoid_synthetic_promises_for_immediate_inputs -- --nocapture`, `cargo test -p mustard rejects_pending_promise_combinator_microtask_source -- --nocapture`, `cargo test -p mustard --test async_runtime -- --nocapture`, `cargo test -p mustard --test property_snapshot_roundtrip generated_suspending_programs_round_trip_snapshots -- --nocapture`, `node --test tests/node/async-runtime.test.js tests/node/async-schedule.test.js`, `cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:rust`, `npm run bench:smoke:release`, `npm run bench:smoke`). Two `npm run bench:workloads:release` reruns stayed mixed and internally inconsistent versus `2026-04-13T20-23-24-663Z-workloads.json`, so this slice is being kept as verified partial progress but the broader Milestone 4 async-clone checkbox stays open and `docs/BENCHMARK_FINDINGS.md` was not updated yet. |
| 2026-04-13T21:15:23Z | `14c4143` (worktree dirty) | Completed the remaining Milestone 6 builtin-helper audit by removing throwaway per-entry `Vec<Value>` materialization from the hot `Object.fromEntries(...)` and `Map` constructor paths: both now clone only the first two entry-array slots through a shared runtime helper instead of collecting whole entry arrays. The retained Rust-core microbench win was `runtime_execution/object_from_entries_hot 3.596-3.638 ms -> 2.913-2.939 ms` (`~19%` faster); `keyed_collection_large/map_ctor_large` moved from `159.94-161.87 ms` to `155.68-157.83 ms` but stayed within noise, so the slice was kept for the clear `Object.fromEntries` win rather than overstating broader collection-constructor gains. | Verification passed (`cargo fmt --all`, `cargo test --workspace`, `npm test`, `npm run lint`, `node --test tests/node/keyed-collections.test.js tests/node/builtins.test.js`, `cargo bench -p mustard --bench runtime_core -- --noplot object_from_entries_hot`, `cargo bench -p mustard --bench runtime_core -- --noplot map_ctor_large`, `cargo bench -p mustard --bench runtime_core -- --noplot collection_callback_hot`). An attempted follow-on tweak to replace per-iteration `Map.prototype.forEach` / `Set.prototype.forEach` callback error-string formatting with static messages regressed `runtime_execution/collection_callback_hot` by about `+5.3%`, so that subchange was reverted before landing; the final `collection_callback_hot` rerun landed back within noise. |
| 2026-04-13T21:37:36Z | `633427a` (worktree dirty) | Landed another partial Milestone 4 async-clone reduction slice by changing queued `ResumeAsync` and `PromiseReaction` microtasks to retain the settled source `PromiseKey` instead of embedding cloned `PromiseOutcome`s, then updating GC root marking plus snapshot validation so queued async work still keeps settled promises alive and forged pending-source microtasks fail closed. Added focused Rust coverage for the new source-backed snapshot validation path and GC retention. The direct Rust-core signal was mixed but relevant: `runtime_execution/promise_all_immediate_fanout` improved from about `870-874 µs` to `845-855 µs` (`~2-3%` faster) while `runtime_execution/vm_hot_loop` improved by about `~1.7%`; `promise_all_settled_immediate` regressed by about `~2.0%`, so the broader async-clone checkbox remains open. A same-machine control rerun against clean `f7bbbce` in `/private/tmp/mustardscript-control5` kept the addon workload evidence mixed: `warm_run_small 0.99 ms -> 0.94 ms` (`-4.7%`), `cold_start_small 1.05 ms -> 1.01 ms` (`-4.4%`), and `startInputs.large 1.19 ms -> 1.05 ms` (`-12.2%`) improved, while `programmatic_tool_workflow 1.48 ms -> 1.54 ms` (`+4.6%`), `host_fanout_100 0.39 ms -> 0.41 ms` (`+6.7%`), `warm_run_code_mode_search 0.51 ms -> 0.54 ms` (`+5.5%`), and `Progress.load_only 0.14 ms -> 0.23 ms` (`+65.0%`) regressed. | Verification passed (`cargo fmt --all`, `cargo test -p mustard rejects_invalid_microtask_frame_state -- --nocapture`, `cargo test -p mustard rejects_pending_promise_reaction_microtask_source -- --nocapture`, `cargo test -p mustard queued_promise_reaction_microtasks_keep_settled_sources_alive_for_gc -- --nocapture`, `cargo test -p mustard --test async_runtime -- --nocapture`, `cargo test -p mustard --test property_snapshot_roundtrip generated_suspending_programs_round_trip_snapshots -- --nocapture`, `cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:rust`, `npm run bench:workloads:release`, `npm run bench:smoke`, `npm run bench:smoke:release`). The first `npm run bench:smoke:release` attempt failed narrowly with `snapshot median ratio 7.66 exceeded 7.5`; an immediate sequential rerun passed at `6.88x`, so this was treated as smoke variance rather than a persistent blocker. `docs/BENCHMARK_FINDINGS.md` was not updated because the same-machine workload signal stayed mixed and the Milestone 4 async-clone checkbox is still open. |
| 2026-04-13T22:11:32Z | `de1cfa1` (worktree dirty) | Audited the still-open Milestone 8 structural-lowering item and landed the first concrete object-literal slice on top of a clean `de1cfa1` baseline: static-key object literals now lower directly to `MakeObject { keys }` instead of `MakeObject {}` plus per-property `Dup` / `SetPropStatic` / `Pop`, object spread entries stop paying the extra duplicate/pop pair around `CopyDataProperties`, `tests/golden/bytecode/make-total.json` now reflects the leaner bytecode, and `static_object_literals_lower_without_runtime_property_mutation_ops` in `crates/mustard/tests/runtime_correctness.rs` locks in the structural guarantee. A same-machine control rerun against `/private/tmp/mustardscript-control6` showed the clearest addon wins on object-heavy code-mode paths and medium/large boundary work: `warm_run_code_mode_search 0.51 ms -> 0.42 ms` (`-17.0%`), `cold_start_code_mode_search 0.90 ms -> 0.80 ms` (`-11.2%`), `startInputs.large 1.07 ms -> 0.97 ms` (`-9.4%`), `suspendedArgs.medium 0.63 ms -> 0.59 ms` (`-5.2%`), and `programmatic_tool_workflow` effectively flat (`1.49 ms -> 1.50 ms`, `+0.2%`). Rust-core benches also moved the right way, including `compile_pipeline/lower_bytecode_small_source ~-7.8%`, `vm_hot_loop ~-15.9%`, `property_access_hot ~-63%`, `object_from_entries_hot ~-45%`, and `snapshot_load_suspended ~-35%`. | Verification passed (`cargo fmt --all`, `cargo test -p mustard --test golden bytecode_lowering_matches_golden -- --nocapture`, `cargo test -p mustard --test runtime_correctness -- --nocapture`, `npm run bench:rust`, `npm run bench:workloads:release`, same-machine compare against `/private/tmp/mustardscript-control6`, `npm run bench:smoke:release`, `cargo test --workspace`, `npm test`, `npm run lint`). An earlier runtime-wide `Arc<str>`/shared-string experiment was benchmark-negative on tracked addon workloads (`warm_run_small`, `execution_only_small`, and `programmatic_tool_workflow`) and was fully reverted before landing this slice. The remaining Milestone 8 action item is still open because destructuring and `for...of` lowering have not been tightened yet, and some same-machine addon medians stayed mixed or noisy (`execution_only_small +2.5%`, `host_fanout_50 +13.1%`, `resumeValues.medium +10.3%` on the kept rerun), so this is recorded as verified partial progress rather than a completed milestone. |
| 2026-04-13T22:31:53Z | `e90f9a0` (worktree dirty) | Landed the first explicit Milestone 8 peephole-specialization slice by adding discard-form `Store*` / `SetProp*` bytecode, a post-lowering remap pass that rewrites statement-context `Store*` / `SetProp*` + `Pop` pairs into those discard opcodes, corresponding VM / validation / GC support, a serialized-program/snapshot format bump to version `2`, and `assignment_statements_lower_to_discard_store_and_set_opcodes` in `crates/mustard/tests/runtime_correctness.rs` to lock in the structural guarantee. Checked in `benchmarks/results/2026-04-13T22-28-11-723Z-workloads.json` and `benchmarks/results/2026-04-13T22-28-35-619Z-smoke-release.json`. A same-machine control rerun against clean `e90f9a0` in `/private/tmp/mustardscript-control7` kept the main addon medians moving the right way on `warm_run_small 0.95 ms -> 0.90 ms` (`-4.8%`), `cold_start_small 1.02 ms -> 0.96 ms` (`-6.0%`), `warm_run_code_mode_search 0.45 ms -> 0.45 ms` (`-1.6%`), `programmatic_tool_workflow 1.50 ms -> 1.49 ms` (`-1.3%`), and `startInputs.medium 0.41 ms -> 0.34 ms` (`-17.0%`), while `execution_only_small` stayed near flat (`2.16 ms -> 2.18 ms`, `+1.1%`) and `cold_start_code_mode_search` regressed modestly (`0.82 ms -> 0.86 ms`, `+5.5%`). | Verification passed (`cargo fmt --all`, `cargo test -p mustard --test runtime_correctness -- --nocapture`, `cargo test -p mustard --test iteration -- --nocapture`, `npm run bench:rust`, `npm run bench:workloads:release`, same-machine compare against `/private/tmp/mustardscript-control7`, `npm run bench:smoke:release`, `cargo test --workspace`, `npm test`, `npm run lint`). A broader stack-based destructuring-lowering experiment tried immediately before this slice was benchmark-negative on the same-machine compare (`programmatic_tool_workflow +2.7%`, `execution_only_small +6.6%`) and was fully reverted before landing the discard-op pass. The broader Milestone 8 peephole item stays open because this only removes statement-result store/set churn so far, and resume-error / suspended-arg surfaces plus `cold_start_code_mode_search` stayed mixed. |
| 2026-04-13T22:40:33Z | `7b5b387` (worktree dirty) | Audited the still-open Milestone 8 inline-cache evaluation item against the latest kept `7b5b387` release artifacts and refreshed `docs/BENCHMARK_FINDINGS.md` to use `benchmarks/results/2026-04-13T22-28-11-723Z-workloads.json` plus `2026-04-13T22-28-35-619Z-smoke-release.json`. The current evidence shows static property dispatch is no longer the dominant remaining bottleneck: addon `warm_run_code_mode_search` is already `0.45 ms`, while the larger residual gaps sit in boundary and sidecar surfaces (`programmatic_tool_workflow 1.49 ms`, `host_fanout_100 0.41 ms`, sidecar/addon ratios `warm_run_small 1.53x`, `programmatic_tool_workflow 11.24x`, `host_fanout_100 14.93x`). Based on that evidence, the plan now marks the monomorphic static-property inline-cache item complete as an evaluated-and-deferred path rather than an implementation target. | No new code-path verification was required because this slice was an evidence/documentation decision over already-verified kept artifacts. Two additional Milestone 8 experiments tried after `7b5b387` (keep-object setters for dynamic object literals and discard-lowering for unused update expressions) were benchmark-mixed on same-machine reruns and were fully reverted instead of being committed, reinforcing that the next higher-value path remains boundary / async-clone / sidecar work rather than deeper property-dispatch specialization. |
| 2026-04-13T22:44:52Z | `fab23db` (no new commit; experiment reverted) | Audited another concrete Milestone 5 addon-boundary slice by replacing the hot `encodeStructuredInputs(...)`, `encodeStartOptions(...)`, and resume-payload helpers in `lib/structured.ts` with a direct JSON-string emitter that preserved the existing DTO wire shape without building recursive intermediate JS objects first. Added focused Node coverage in `tests/node/structured-encoding.test.js` and reran the relevant host-boundary/security suites plus `npm run build`; all functional checks passed before the benchmark gate. | The release workload evidence moved the tracked addon medians the wrong way, so the direct-string encoder was fully reverted instead of being committed. Versus the latest kept `7b5b387` artifact `benchmarks/results/2026-04-13T22-28-11-723Z-workloads.json`, the candidate `2026-04-13T22-44-10-134Z-workloads.json` regressed on `programmatic_tool_workflow 1.49 ms -> 1.60 ms` (`+7.9%`), `warm_run_small 0.90 ms -> 0.93 ms` (`+2.5%`), `host_fanout_100 0.41 ms -> 0.43 ms` (`+6.1%`), `startInputs.medium 0.34 ms -> 0.40 ms` (`+17.3%`), and `startInputs.small 0.09 ms -> 0.16 ms` (`+78.2%`) even though some resume/suspended-arg surfaces improved. No external blocker was identified; this strongly suggests the JS-side manual string builder loses to the engine’s native `JSON.stringify` fast path and should not be retried as-is. |
| 2026-04-13T23:06:23Z | `4d57270` (worktree dirty) | Completed the remaining Milestone 7 wire-format action items by switching `mustard-sidecar` to default length-prefixed binary frames with a JSON header plus raw program/snapshot payload bytes, keeping explicit `--jsonl` debug mode, bumping the sidecar protocol version to `2`, updating Rust protocol/hardening coverage plus Node sidecar/package smoke helpers, refreshing `docs/SIDECAR_PROTOCOL.md` and `docs/BENCHMARK_FINDINGS.md`, and checking in `benchmarks/results/2026-04-13T22-59-34-874Z-workloads.json` plus `2026-04-13T23-00-15-361Z-smoke-release.json`. Relative to the previous kept sidecar artifact `2026-04-13T20-03-16-697Z`, the new kept artifact improved sidecar `startup_only 9.45 ms -> 3.66 ms` (`-61.3%`), `cold_start_small 6.17 ms -> 4.92 ms` (`-20.2%`), `cold_start_code_mode_search 6.28 ms -> 4.77 ms` (`-24.0%`), `warm_run_code_mode_search 0.82 ms -> 0.66 ms` (`-20.0%`), `programmatic_tool_workflow 17.20 ms -> 16.78 ms` (`-2.5%`), `host_fanout_100 6.41 ms -> 6.12 ms` (`-4.7%`), and `transport_resume_only 0.10 ms -> 0.09 ms` (`-10.2%`), while `warm_run_small` stayed roughly flat (`+1.1%`) and `host_fanout_1` regressed (`0.12 ms -> 0.41 ms`, `+230.8%`). | Verification passed (`cargo fmt --all`, `cargo test -p mustard-sidecar --test protocol --test protocol_state --test hostile_protocol -- --nocapture`, `npm run build && node --test tests/node/sidecar-equivalence.test.js`, `npm run bench:workloads:release`, `npm run bench:smoke:release`, `cargo test --workspace`, `npm test`, `npm run lint`). The first `npm run bench:workloads:release` attempt failed because one failure-path benchmark still expected the old request helper shape; fixing `benchmarks/workloads.ts` and rerunning resolved it. The first `npm run bench:smoke:release` attempt failed narrowly with `host-call median ratio 5.20 exceeded 5`; an immediate sequential rerun passed at `4.39x`, so this was treated as smoke variance rather than a persistent blocker. The Milestone 7 action items are now complete, but the sidecar/addon ratio targets are still far from `<= 1.20x`, so broader performance work remains open. |
| 2026-04-13T23:33:08Z | `69424f8` | Audited the still-open Milestone 5 JS structured-boundary DTO hot path and kept a narrower `lib/structured.ts` slice: `enumerateDataProperties(...)` now walks `Object.keys(...)` and fetches per-key descriptors instead of materializing a full `Object.getOwnPropertyDescriptors(...)` table plus `Object.entries(...)` array before Rust sees the payload. Existing Node boundary/security coverage already locked in the critical fail-closed rules for accessors, proxies, `__proto__`, sparse arrays, and property order, so no semantic/API docs changed. Checked in `benchmarks/results/2026-04-13T23-27-25-582Z-workloads.json` plus `2026-04-13T23-32-13-717Z-smoke-release.json`. Relative to the latest kept workload artifact `2026-04-13T22-59-34-874Z`, the direct addon boundary medians improved on `startInputs.large 0.95 ms -> 0.80 ms` (`-16.3%`), `resumeErrors.large 0.92 ms -> 0.81 ms` (`-11.7%`), `resumeErrors.medium 0.30 ms -> 0.27 ms` (`-10.2%`), `resumeValues.large 0.99 ms -> 0.95 ms` (`-3.7%`), `suspendedArgs.large 2.16 ms -> 2.08 ms` (`-3.8%`), and `suspend_resume_20 2.37 ms -> 2.27 ms` (`-4.2%`). | Verification passed (`node --test tests/node/host-boundary.test.js tests/node/security-host-boundary.test.js tests/node/property-boundary.test.js tests/node/execution-context.test.js`, `npm run build`, `cargo test --workspace`, `npm test`, `npm run lint`, `npm run bench:rust`, `npm run bench:workloads:release`, `npm run bench:smoke:release`). `npm run bench:rust` stayed mostly mixed/noisy as expected for a JS-only slice, and the broader release workload signal remained mixed (`warm_run_small +5.4%`, `warm_run_code_mode_search +7.1%`, `execution_only_small +11.6%`, `programmatic_tool_workflow +1.0%`, `host_fanout_100 +0.6%`), so the remaining Milestone 5 checkbox stays open and `docs/BENCHMARK_FINDINGS.md` was not updated yet. No external blocker was identified. |
